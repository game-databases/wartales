# Wartales Database — Live Progress

Live progress page for the Wartales pack (`wartales/`). Updated by the
orchestrator after every completed unit. Question queue for owner-gated
items lives at the bottom.

- **Game:** Wartales (Shiro Games, Heaps.io/HashLink engine)
- **Client:** `A:\SteamLibrary\steamapps\common\Wartales\` · Steam appid
  `1527950` · buildid `20318128`
- **Host:** NE8K (this machine holds both the repo and the client)
- **Started:** 2026-08-24 (fresh pack bootstrap; previous orchestrator was
  fully stopped — no prior state existed)

---

## Phase board

| Phase | Status |
|---|---|
| P0 Bootstrap & research (recon, toolchain, competitors, locales, spec) | **DONE — spec FROZEN 2026-08-24** (arbiter delta APPROVE-FOR-FREEZE) |
| P1 Extraction: harvest → decompile → datasets → relink → PROOF | **NEARLY CLOSED** — run_all stages 1–6 BUILT+SHIPPED (`af29425a`). Residue ledger: R1 HBSON **CLOSED** (dig 7) · R2 achievements **RESOLVED** keyless (dig 10) · R3/R3b disassembler+decompiler **CLOSED** (digs 8+13) · R6 sdlboot **CLOSED** (dig 9) · R7 battle terrain **CLOSED** (dig 12) · R8 world↔tile transform **CLOSED** (dig 11) · **R10 relink matrix LANDED** (dig 15: 7,184 edges / 107 families / 0 dangling, triple-verified PASS·SOUND·PASS) · **R9 locale wave-2 in delta verification** (dig 14 fix round complete: 217,611 cells / fallback class emitted / wave-1 byte-intact) · **R5 fr stamping delivered** (dig 14 meta table). Remaining: dig-14 delta verdict → landing |
| P2 Data-completeness gate (rule 8 arbiter) | fires on dig-14 landing — the only remaining P1 item |
| P3 Site build (DB, map, builder, calculators, guides/news, i18n, UGC, SEO/AEO) | F1 tokens DONE; **all four foundation specs BUILD-READY through full dialectical chains** (F4 approve+r1b+r2b · PT16 approve · F5 r1→arbiter→r2 executed-green · F2 13/13+probes+r2 carrier-unified); builds launch behind the P2 gate |
| P4 Visual critic loop vs references + e2e smoother | not started |
| P5 Definition-of-done verification + missingdata.md | not started |

## Execution plan — subagent launch batches (owner directive 2026-08-26)

The task board IS the plan; batches below say what launches **together in
parallel** for maximum subagent utilization. Every piece still goes
through the full pipeline law (PrepareTask → blind build → double review
→ arbiter → fix loop).

- **Batch 1 — data closure + spec prep (parallel, ~6 agents):**
  dig 14 locale wave‑2 (R9+R5) ∥ dig 15 relink matrix (R10) ∥
  Documentator specs for F2 contracts · F4 app shell · F5 search ·
  PT16 SEO/AEO. Spec-writing is planning, not frontend — legal before
  the rule‑8 gate; builds stay gated.
- **Batch 2 — gate:** verify + commit digs → **rule‑8 completeness
  arbiter** (#13). F2 spec review chain can run in parallel.
- **Batch 3 — foundations (after APPROVE, parallel pairs):**
  CodeWriter+TestWriter for F4 shell → then F5 search ∥ PT16 SEO ∥
  icon CDN (∥-safe, disjoint surfaces). Reviewers per piece.
- **Batch 4 — DB core (parallel):** PT02 taxonomy ∥ PT03 lists ∥
  PT04 entity anatomy/tabs — all consume F2 contracts + canonical data.
  Critic wave 1 (home/lists/entity) follows each landing.
- **Batch 5 — map:** PT05 /map page → PT06 inline modules (06 needs 05's
  module primitive). Critic wave 2a.
- **Batch 6 — flagship + tools:** PT07 builder MVP ∥ interrogation DB ∥
  recipe explorer ∥ food calc (all formulas now decoded); then trade
  planner+atlas. PT08 gallery rides builder. Critic wave 2b.
- **Batch 7 — content + UGC:** PT10 guides ∥ PT11 news+diff ∥ PT12 lore
  ∥ PT13 quests ∥ PT14 trackers ∥ PT15 UGC ∥ PT17/PT18. e2e smoother
  between this and earlier waves.
- **Standing ops:** patch watch TargetBuildID 21238928 → rerun pipeline
  = first patch-diff dataset; CWV budgets enforce per template as pages
  land; DoD sweep + missingdata.md last.

## Log

### 2026-08-26 (morning) — Batch 1 spec chains closed; dig 15 landed; dig 14 in delta

- **All four foundation specs reached BUILD-READY through the full
  pipeline law**, each with its own reviewer→fixer→arbiter chain and
  cross-spec arbitration: F4 shell (12 findings + pt-BR errata ruling +
  smoke-leg staging), PT16 SEO (12 findings + collision adjudication —
  `pageSegments` manifest carrier law born here), F5 search (12
  findings + ko-jamo prefix law executed codepoint-green + `/db`-route
  conformance), F2 contracts (13 findings incl. post-dig-14 recount
  governance, true id charset `[A-Za-z0-9_-]` with env-/recipe-
  hyphen families, carrier unified to F4's law both-directions
  asserted).
- **DIG 15 LANDED** (`8c62386`): relink matrix upgrade at evidence
  layer — 1,560 ordered directions fully classified, **7,184 edges /
  107 families / 0 dangling** (52 typed directions emitted from CDB
  hidden sub-sheets during the fix round), sampler 107/107, all
  locators survived independent break attempts, PROOF R10 truthful.
- **Dig 14 fix round complete, in delta verification**: BLOCKER resolved
  (refless struct-column prose now emits via fallback patterns — 7,083
  cells that audits caught being silently dropped), flatten_struct
  counters fixed (+12,295 cells recovered exactly), UI inventory
  completed (40+197 groups / en 2,148 strings), fillers = exactly 3,
  headline recomputed to **217,611 game-content + 19,081 UI cells**,
  wave-1's 27 overlays byte-identical throughout. Availability now
  14,456 rows = 9,827 bridged + 4,629 drift.
- Drive emergency (C: 454 MB) hit and was resolved by owner cleanup →
  ~40 GB free; Q2 closed out.

### 2026-08-26 — Batch 1 airborne; R3b closed; drive pressure escalated

- **Six parallel subagents launched** (Agent tool, per owner directive):
  dig 14 locale wave-2 (R9, reconciles prior partial scratch), dig 15
  relink matrix (R10, startChoice.props.pattern edge family seeded),
  and Documentator specs for F2 published contracts / F4 app shell /
  F5 header search / PT16 SEO-AEO. Spec-writing is planning, legal
  before the rule-8 gate; builds stay gated on #13.
- **Dig 13 verified landed**: `extracted/decompiled/hl-src/` structured
  text for every function (capture odds f#7774, damage preview f#8320
  readable); constant-name strings join code ↔ ConstantKind rows.
  Closure call-site typing (8,964 sites) ledgered as next decompile
  upgrade; heavy emits staged on D: when C: filled mid-dig.
- **Toolchain committed into the standalone repo**: pipeline/tools code
  + pinned binaries (~4.6 MB) now tracked so run_all is reproducible
  from a fresh clone; `_verify/` fixtures stay local (game-derived,
  rule 5); `.claude/` session state ignored.
- **C: pressure worsened: 2.2 GB free at 100%** (was 7.7 GB). Safe lever
  pulled (npm cache clean, +0.3 GB); real fix needs the owner call on
  cross-pack Temp scratch — Q2 escalated to urgent.

### 2026-08-25 — wave 1+2 results: R1/R2/R6/R7/R8 closed; stages built under review

- **Dig 7 HBSON decoder — R1 CLOSED** (top unblock since Dig 1): grammar
  reversed with three-oracle agreement (`docs/hbson-format.mdx`);
  3,536/3,536 blobs zero-slack + byte-exact re-encode + independent
  second implementation; deferred kinds emitted (place 536 / group 791 /
  element 5,180 / levelProps 5,982); **607 POI/town/secret coordinates**;
  layers2D value→id tables; basePrice = measured negative (no carrier;
  fallback is dead code); all 72 unverifiable relink edges flipped
  valid:true.
- **Dig 8 disassembler — R3 complete**: every function operand-resolved
  (52,114 fns / 2,508,050 ops → 1,584 `.dis.hx` mirrors); op-28 decoded;
  CFG 504k blocks; **capture odds fully decoded**, XP composition, trade
  distance gradient w/ 50-unit free radius, guard cap/pierce/backstab,
  MaxLevelLock=15 semantics. Structured-text decompiler = dig 13 running.
- **Dig 9 sdlboot**: same game linked vs SDL/GL backend (92.66% opcode
  mass identical); nothing game-mechanics-unique — hlboot stays canonical.
- **Dig 10 achievements — R2 RESOLVED keyless**: 235 rows via community
  stats + keyless global-percent endpoint; id alignment solver (tie-block
  bipartite matching); Steam locales proven en-only for achievements
  (honest availability rows, no fake overlays); client join 212/216;
  soundtrack app 3533110 first enumerated non-content app.
- **Dig 11 world↔tile transform — R8 CLOSED**: single global affine
  px=wx·4, py=wy·4 from carrier arithmetic (not raster fitting);
  509/518 marker→region labels via the client's own formula; maps.json
  `coordinateTransform` pinned; AC9 roundtrip budget frozen; pytest 93/0.
- **Dig 12 battle terrain — R7 CLOSED**: 97 scenes × 1,470 tiles × 4
  roles (h/n/i/w), 64 m tiles double-derived vs engine `Terrain.hx`,
  normals proven (R²=0.997), **tactical cell = 1 m measured** across 230
  arena markers; CDB join 108/108.
- **Stages 4–6 BUILT through the full PrepareTask chain**: spec drafted
  (re-measured numbers) → reviewer NEEDS_REVISION (3 blockers) → arbiter
  REVISE F1–F14 → fixer applied w/ proofs → arbiter r2 **APPROVE-FOR-
  BUILD** → blind parallel CodeWriter (4 tools + wiring, self-tests green)
  + TestWriter (54 cases, 34+20) landed. Fresh CodeReviewer +
  TestReviewer now running; fix loop next per verdicts.
- Gateway flakiness tonight: repeated 429/empty-200 agent deaths — all
  recovered by fresh relaunches, zero work lost.

### 2026-08-25 — orchestrator relaunch (previous orchestrator fully stopped) + wave 1

- **State reconciled from disk** (PROGRESS was stale): run_all stage 1
  harvest + stage 2 map registry BUILT and committed (`ff66d0d`, 62-test
  map suite green; 1,836 albedo tiles); datasets drafts complete for all
  40 CDB kinds (10,207 rows + 1,266 constants = census closure, verifier
  proven); relink seeds 51 files / 19,130 edges / 0 dangling; locale
  bridge ×9 locales proven (55,350/55,350 cells, zero fillers);
  hl-structure complete (zero-slack + crashlink cross-check); PROOF.md
  drafted with residue ledger R1–R10; F1 tokens **done** per critic r4.
- **Wave 1 launched** (4 parallel agents, briefs in `docs/briefs/`):
  Dig 7 HBSON prefab decoder (R1 — gates deferred kinds, POI/town coords,
  worldmap overlays, basePrice), Dig 8 HL operand disassembler (R3 step —
  gates formula recovery for calculators), stages 4–6 Documentator spec
  (datasets/relink/emit promotion into run_all), tokens r5 fixer (r4
  carried hygiene: texture staging verify + panel-alpha-over-shot-20
  proof).
- Requirement chain re-read in full by the new orchestrator (AGENTS.md,
  decision-register, domain/site-sections/seo/localization/design/
  extraction doctrine+host, FRAMEWORK §2/§6–§8, spec.md, tools-plan.md).

### 2026-08-24 — scout wave results

- **Client recon delivered** (`docs/client-recon.mdx`, verified on disk):
  7,089 files / 48 GB reconciled to Steam manifest within 1 byte; Shiro
  `PAK\0` format fully decoded read-only (no encryption/compression, two
  data regions, Σsizes == span, delta 0 on all four paks); 40,201 packed
  entries inventoried (assets 17,131 · content 15,248 · map 6,886 ·
  res 936) with reusable TSVs; **game brain in res.pak** — `data.cdb`
  CastleDB 6 MB + `formulas.hx` + 12 hscript sources + `names.yml`;
  **9 client locales verified** (de/en/es/fr/ko/pl/pt-BR/ru/zh, master
  texts.xml is French — dev pivot); `export_*.xml` CDB export = 35
  sheets / 9,912 text leaves = entity↔text relink bridge; hlboot.dat =
  HLB v4 ~16 MB logic layer; audio loose in res/audio (Wwise 2.49 GiB,
  clean offload set); zero video; .dds 31.7 GB map tiles inside paks.
- **Planning risk recorded**: Steam queues patch TargetBuildID 21238928
  (~3.66 GB). Decision: proceed against pinned buildid 20318128 now;
  pipeline re-runs on the new build when it lands → first patch-diff
  dataset for free.
- **Design sources delivered** (`design/sources/`, INDEX tracked, shots
  local): 20 verified official UI shots covering inventory, character,
  combat, both maps, fief, tavern, arena, naval, dialogue, logo, camp
  texture. Rarity = slot background tints on near-black quilted panels;
  serif-first medieval typography; gold reserved for currency/selection.
- **Documentator launched** (detached) for `spec.md` draft per FRAMEWORK
  §4; competitor + toolchain reports still in flight → [PENDING-SCOUT]
  holes expected in first draft.

### 2026-08-24 — bootstrap

- Requirement chain read in full: root AGENTS.md, decision-register,
  domain-doctrine, site-sections, seo-standard, localization-architecture,
  design-standard, extraction-doctrine, extraction-host, FRAMEWORK §2/§4/§6/§7/§8,
  foundation-guide, NWG deep-dive, enka feel notes.
- Client located and identified: **Heaps.io/HashLink** (not Unity) — four
  `.pak` archives (`assets/content/map/res`), `hlboot.dat` bytecode,
  `texts_en.xml` + `export_en.xml` at root. Buildid pinned above.
- No prior Heaps writeup exists in the knowledge base → toolchain is
  net-new work; a KB writeup ships with it.
- Pack scaffold created (`contracts/ data/ pipeline/ output/ design/
  docs/ site/`); master task list stood up (29 tracked tasks).
- Piece decomposition shipped (`docs/pieces.mdx`): F1–F5 foundations +
  PT01–PT18 page-type pieces.
- Scout wave running (4 parallel agents): client recon, toolchain
  validation, competitor research, design-source gathering.
- Note#5 substitution recorded: `codex` CLI absent → bailian `bl image`
  is the image-generation fallback.

### 2026-08-24 — toolchain + tools-plan finals

- **Toolchain R2 verdicts**: wtpak.py reconciles 0-mismatch against all
  40,201 recon rows; extractions proven past the 4 GiB boundary
  (1,612 map.pak entries); PAKTool cross-proven byte-identical
  (secondary); no crypto gate anywhere. **Wartales ships a HashLink
  fork** — public hlbc fails on novel type-kinds ≥19; seeded probe
  (`hlboot_probe.py`) strict-parses the header and decodes all 75,321
  strings (52,114 functions / 46,891 types / 34,258 globals); the
  types-walk continuation is mechanical. **`data.cdb` extracted:
  CastleDB JSON, 386 sheets — data-layer core readable as-is.**
- **Tools-plan finals**: market vacuum — no dedicated Wartales tooling
  observable anywhere. Top-5: company builder (flagship MVP =
  single-companion editor, shareable-URL state, computed from
  data.cdb + decompiled formulas), trade route planner + price atlas,
  owned map, recipe/blueprint explorer, interrogation DB. 20 mechanics
  enumerated; P0-DIG cells = the moat.

### 2026-08-24 — review pass + parallel digs

- **Both spec reviews delivered**: spec review = 11 findings / 0
  blockers / ~30 traced claims all clean → NEEDS-REVISION (editing
  pass only). Harvest review = 1 PROVEN blocker: the recon TSVs'
  flag-2 offset column is synthetic (0/N live matches, values step by
  2²⁸) → reconciliation contract moves to (path, size, adler);
  pipeline design otherwise sound.
- **Arbiter running** on both artifacts (verdicts → freeze / build).
- **Three parallel data digs launched** (all unblocked by earlier
  verdicts): ① hlboot types-walk continuation (fork's novel
  type-kinds, zero-slack proof standard, structure registries to
  `extracted/logic/hl-structure/`); ② F1 design-tokens spec
  (Documentator extracts the game's own `ui/style.css` + `.asl` via
  wtpak.py first — owner's shadcn-upgrade directive is the canon);
  ③ CastleDB census (`data.cdb` already extracted: 386 sheets) →
  kind map → first three canonical JSONL kinds + hard `ref`-edge
  relink seeds.

### 2026-08-24 — harvest build approved by reviewers; datasets reach CDB closure

- **Harvest build round 1**: CodeFixer 10/10 (blocker dead with live
  proof), TestFixer suite to **48 passed / 0 failed / 3 skipped**;
  round-2 delta reviews both **CLEAN** — "fit for the full four-pak
  run". Acceptance Arbiter verifying (provider 429s retrying).
- **Datasets: CDB closure proven** — 40 kinds / 10,207 rows + 1,266
  constants = census grand total (11,473, independent verifier);
  relink graph **51 files / 19,130 hard edges / 0 dangling**; deferred
  kinds honestly listed (HBSON-prefab-gated, Steam-surface
  achievements, subset views).
- **Localization: key convention proven exhaustively** — 55,350/55,350
  leaf re-extractions × 9 locales; coverage 82.5–98.9% (limiter =
  engine-internal CDB↔export drift, counted); **zero fillers needed**
  among official locales; fr-export vintage anomaly flagged (patch-diff
  carrier gap).
- **Tokens spec**: arbiter-fixed 8/8; F1 CodeWriter building
  tokens.css + parity tests + critic preview page.
- Ops note: tonight's API gateway intermittently kills agents
  (empty-200s, 429s) — every loss recovered by fresh-continuation
  relaunch; no work lost (file-based tracking).

## Question queue (owner-gated)

| # | Question | Why it blocks | Workaround in flight |
|---|---|---|---|
| Q1 | Domain placement for Wartales (dedicated vs hybrid vs hub path)? Not on the 51-product roster yet. | D3 domain+trademark is an owner-only gate. | Localhost-first build proceeds regardless; provisional roster entry recorded in decision-register. |
| Q2 | **C: drive emergency resolved 08-26**: 11 GB → 454 MB within ~25 min mid-morning (du on `Temp` times out — enormous cross-pack scratch); owner cleanup landed by ~07:30 → **36 GB free**. Orchestrator pull levers (npm/pip cache) contributed ~2 GB transiently. | Was breaking every concurrent NE8K build at <1 GB. | Per-wake free-space recheck stands; this pack keeps scratch in gitignored `output/` + temp-on-D:. Owner-side Temp hygiene remains open for the long term. |

<!-- END OF PROGRESS — updated after every completed unit -->
