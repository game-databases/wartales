# F4 — App shell spec (Next.js scaffold, locale routing, chrome i18n)

**Status:** piece spec, ready to build after the rule-8 gate opens Batch 3
(planning deliverable — spec-writing is legal before the gate,
[`PROGRESS.mdx`](../PROGRESS.mdx) Batch 1); revised r1 2026-08-26 — all
twelve findings of [`review-spec-f4-shell.mdx`](./review-spec-f4-shell.mdx)
resolved, see [Revision r1](#revision-r1); errata r1b 2026-08-26 — the
PT16-arbiter pt-BR collision ruling + F4-shell finding N1, see
[Revision r1b](#revision-r1b-errata). **Authority:**
[DR-2026-08-24-wartales-roster] (pack 52; provisional Umbrella tier;
localhost-first) · owner shadcn directive
([shadcn-adoption.mdx](../design/shadcn-adoption.mdx)) ·
[DR-2026-08-20-locale-urls] (pivot bare paths) ·
[DR-2026-08-22-search-is-not-a-page] (the header field is F5's — this spec
only leaves the slot). **Stack law:** FRAMEWORK §2.20 default profile —
Next.js **App Router** + React 19 + TypeScript; Pages Router and pure SPAs
are excluded.

Scope boundary (pieces.mdx): F4 delivers the **shell** — scaffold, locale
machinery, chrome i18n, header/nav/footer anatomy. It does **not** deliver
the search field (F5), the home page content (PT01), the taxonomy menu that
later deepens the footer (PT02), hreflang/sitemap/JSON-LD (PT16), or the
branded 404 art (PT17 — F4 ships the *mechanism* only).

## 0. Evidence base

- Locales: client-verified ×9 (`res.pak:/lang/{texts,export}_<l>.xml`),
  frozen in [`spec.md`](../spec.md) `locales.official`; pivot EN; filler
  policy `omit-until-translated`; `zh` ships store-named zh-Hans
  ([locale-key-convention.mdx](./locale-key-convention.mdx) §1).
- Token canon (F1, DONE, frozen): [`site/src/styles/tokens.css`](../site/src/styles/tokens.css)
  + `tokens.test.ts` — byte-parity-guarded; **this piece edits neither
  file** (§6). Fonts self-host contract:
  [`site/public/fonts/README.md`](../site/public/fonts/README.md); texture
  staging: [`site/public/assets/ui/README.md`](../site/public/assets/ui/README.md).
- Shell references: NWG shell anatomy
  (`_foundation/nw-site-new-deep-dive.md` §5 — the 3-level taxonomy menu
  drives nav/footer; §9 — explicit routing only, `localeDetection: false`)
  judged against the wikily.gg modern-wiki design language
  (`_foundation/design-standard.md` §2) and the feel bar (§5).
- Existing scaffold: `site/package.json` (`test: vitest run`; devDeps
  jpeg-js + vitest), `site/src/styles/*`, and — as-found, not created here
  (r1) — `site/.gitignore` (`node_modules/`, `package-lock.json`,
  `public/fonts/*.woff2`; this piece only extends it, §9) and
  `site/package-lock.json` (regenerated by install, never hand-edited).
  Everything else under `site/` is created by this piece.

## 1. Scaffold

App Router under `site/src/app/`, TypeScript strict, Tailwind v4
(`@tailwindcss/postcss`), next-intl for locale routing and chrome i18n
(FRAMEWORK §2.20 standard libraries). Next.js 16 line (the network's
reference repo already proves it), React 19 — exact versions pinned in
`package.json` at install.

**Compatibility gate (r1).** The one dependency pair with zero precedent in
the network is next-intl × Next 16: no sibling pack uses next-intl (MiSide
hand-rolls `src/i18n/`) and FRAMEWORK §2.20 sanctions it only generically.
The pairing is therefore proven at install, mechanically: exact versions
pinned; `npm install` must resolve with **zero peer conflicts** between
next-intl and the pinned Next major; and the smoke suite (§7.6) must pass
over the full API surface this spec uses — `defineRouting`,
`createMiddleware`, `localePrefix: "as-needed"`, `localeDetection: false`,
`setRequestLocale`, `createNavigation`. If next-intl's peer range excludes
the pinned Next major, stop before any component work and record the
substitution decision (next-translate — §2.20's alternate — or MiSide-style
hand-rolled routing); the §3 301 pre-shim keeps the DR's redirect law
library-independent either way.

```text
site/
├─ next.config.ts                 # createNextIntlPlugin(); no other customization
├─ tsconfig.json                  # strict; paths: "@/*" -> "src/*"
├─ postcss.config.mjs             # @tailwindcss/postcss
├─ package.json                   # EXTENDED (below) — "test": "vitest run" untouched
├─ messages/                      # chrome i18n, §4 — 9 locale files + stub ledger
├─ scripts/
│  ├─ smoke.mjs                   # §8 executable AC runner (dev-server curl sweep)
│  └─ stage-assets.mjs            # §7 fonts/texture staging per the READMEs
│                                 #   + --check: missing-ref reporter (§7.6, r1)
├─ public/                        # fonts/ + assets/ui/ staged per README (existing)
└─ src/
   ├─ middleware.ts               # pivot-bare enforcement (§3)
   ├─ app/
   │  └─ [locale]/
   │     ├─ layout.tsx            # <html lang>; fonts; globals; provider; shell
   │     ├─ page.tsx              # smoke home — replaced wholesale by PT01
   │     ├─ [...rest]/page.tsx    # catch-all whose only body is notFound() —
   │     │                        #   the mechanism that makes the nested
   │     │                        #   not-found fire for unmatched URLs (B1 r1)
   │     └─ not-found.tsx         # mechanism only — PT17 brands it
   ├─ components/
   │  ├─ ui/                      # shadcn primitives copied in (button, popover, command)
   │  └─ shell/                   # header, nav-row, locale-combobox, search-slot, footer
   ├─ i18n/
   │  ├─ locales.ts               # single source-of-truth locale table (§2)
   │  ├─ routing.ts               # defineRouting — THE segment law (§3)
   │  ├─ request.ts               # getRequestConfig (static imports); calls
   │  │                           #   notFound() on any id outside §2 (m5 r1)
   │  └─ navigation.ts            # createNavigation (Link, redirect, usePathname)
   ├─ lib/
   │  └─ routes-manifest.ts       # nav/footer links; render-only-existing law (§5)
   └─ styles/
      ├─ tokens.css               # F1 CANON — ZERO EDITS
      ├─ tokens.test.ts           # F1 CANON — ZERO EDITS
      └─ globals.css              # NEW — imports tokens.css, wires Tailwind theme (§6)
```

`components.json` (shadcn) lands with the first primitive copy. Primitives
copied at F4: `button` (nav/footer links), `popover` + `command` (locale
combobox). Every primitive is upgraded per shadcn-adoption §layers —
structure/interactions/motion/typography from the F1 canon; a stock-looking
primitive fails the round (adoption §Verification). No vitest.config is
added at F4: new tests stay node-env + relative-import so the existing
zero-config run (`vitest run`) discovers both old and new suites unchanged;
a config arrives with the first component-test piece (F5).

**package.json extension** (add, never rewrite existing keys):

- `scripts`: `dev`, `build`, `start`, `stage`, `smoke` (existing `test`
  byte-identical).
- deps: `next`, `react`, `react-dom`, `next-intl`, `cmdk`,
  `@radix-ui/react-popover`, `clsx`, `class-variance-authority`,
  `tailwind-merge`; devDeps: `typescript`, `tailwindcss` ^4,
  `@tailwindcss/postcss`, `@types/node`, `@types/react`,
  `@types/react-dom`. TanStack Table/Query/Leaflet are §2.20 standards but
  belong to the pieces that consume data — not installed here.

## 2. Locale model — one table, one derivation

Client codes are the extraction truth; URL segments are a presentation
concern ([locale-key-convention.mdx](./locale-key-convention.mdx) §1).
`src/i18n/locales.ts` is the **single** mapping; everything else derives:

| id = URL segment | client code | BCP-47 (`<html lang>`, hreflang) | native name (switcher) |
|---|---|---|---|
| `en` | `en` | `en` | English — **pivot, bare paths** |
| `de` | `de` | `de` | Deutsch |
| `es` | `es` | `es` | Español |
| `fr` | `fr` | `fr` | Français |
| `ko` | `ko` | `ko` | 한국어 |
| `pl` | `pl` | `pl` | Polski |
| `pt-br` | `pt-BR` | `pt-BR` | Português (Brasil) |
| `ru` | `ru` | `ru` | Русский |
| `zh` | `zh` | `zh-Hans` | 简体中文 |

- **Recorded decision (r1): lowercase `pt-br` URL segment — override
  EXERCISED (r1b), segment unchanged.**
  [DR-2026-08-20-locale-urls] ¶2 illustrates regional forks as `/pt-BR/…`
  but fixes only membership and the bare-path law, not letter case — the
  divergence is deliberate and recorded here, not left a spec-local
  default. Reasons: lowercase-only URL segments (hygiene; no mixed-case
  hrefs), hreflang/`<html lang>` carry the true `pt-BR`, and the page
  segment is a presentation id that never crosses into data-plane URLs —
  those use client codes verbatim per
  [`spec-f2-contracts.mdx`](./spec-f2-contracts.mdx) §4 (`/data/pt-BR/…`),
  with `locales.ts` the single id↔client-code bridge between the two
  planes. This paragraph reserved an orchestrator override before first
  deploy ("flipping this §2 row is the whole change"); the PT16-arbiter
  pt-BR collision adjudication
  ([`arbiter-pt16-seo.mdx`](./arbiter-pt16-seo.mdx)) is that override,
  exercised on F4's side WITHOUT flipping the id: the page segment stays
  lowercase `pt-br`, and the load-bearing cell is `client code` — see the
  composition law below.
- **Data-plane composition law (r1b — the collision's F4 half).** The two
  planes are joined ONLY through this table: page tree = the `id` column,
  data tree = the `client code` column. A consumer turning a page path
  into a data URL resolves the stripped leading segment through the table
  (or its manifest projection below) BEFORE composing — applying F2 §6's
  unconditioned formula verbatim to `/pt-br/item/Gold` would yield
  `/data/pt-br/item/Gold.json`, which is outside F2's declared locale set
  and 404s against F2's own `/data/pt-BR/…` plane. Links are therefore
  built manifest-first — enumerate the table's locales, construct
  `/data/{clientCode}/{kind}/{id}.json` from the record — never by string
  surgery on page paths.
- **What `api.manifest.json` must carry (r1b; stated here because F2 is
  frozen and is not edited by this errata).** F4's table alone cannot
  guarantee F2 §6 composes correctly: F2 §15 AC8 freezes
  `locales.prefixed` to the eight CLIENT codes while F2 §6's formula
  consumes the raw stripped segment, so a manifest holding only client
  codes cannot resolve `pt-br` to `pt-BR`. The manifest's `locales`
  object therefore carries one additive key:
  `"pageSegments": {"pt-br": "pt-BR"}` — key = page segment, value =
  client code; exactly one entry today, since every other prefixed code
  equals its own page segment. Segment resolution for any consumer: a
  stripped segment that is a member of `prefixed` IS its own client code;
  a member of the `pageSegments` keys takes the mapped value; neither →
  not a locale segment (F2 AC7's non-locale semantics preserved — AC7 and
  AC8 probe client-code spellings and stay green unmodified).
- `ja` (community-overlay candidate, spec `locales.community`) is **not**
  site-facing at F4 — its ship call is a build-time decision per the frozen
  spec; adding it is a table row + one messages file, nothing else.
- Message files and router locale ids both use the `id` column; joins to
  `extracted/` overlays (client codes) go through `clientCode`. No second
  mapping may appear anywhere in `site/`.

## 3. Routing — the exact middleware/segment strategy

`src/i18n/routing.ts`:

```ts
defineRouting({
  // ids are the §2 PAGE segments (id column); /data/** URLs compose from
  // the §2 client-code column, never from these strings (§2 r1b).
  locales: ["en", "fr", "de", "es", "pl", "pt-br", "ru", "ko", "zh"],
  defaultLocale: "en",
  localePrefix: "as-needed",   // pivot bare, others prefixed
  localeDetection: false,      // explicit links only (NWG §9; seo-standard §1)
});
```

`src/middleware.ts` wraps `createMiddleware(routing)` with one **pre-shim**
that guarantees the DR's permanent-redirect clause regardless of library
default: any pathname equal to `/en` or starting `/en/` answers
**301** → the same path stripped of the `/en` prefix. Rules pinned:

1. **Pivot bare.** `/` and all pivot pages carry no locale segment;
   canonical forms are bare ([DR-2026-08-20-locale-urls] ¶1).
2. **`/en/*` never exists.** It 301s to the unprefixed path (never 302,
   never a duplicate render). No internal `<a>`/`Link` may ever emit `/en`.
3. **Eight prefixed locales**, fully localized pages, one locale's text per
   page — never mixed (localization-architecture §1).
4. **No negotiation.** No `Accept-Language`/geo redirect, ever: a curl of
   `/` with `Accept-Language: fr` returns the EN page at `/`
   (seo-standard §1: no crawler-blocking geo-redirects; NWG explicit-routing
   precedent). Discovery of other locales is the switcher + link rows.
5. **Unknown locale segment** (`/xx/...`) is a 404, not a redirect —
   enforced mechanically, not assumed (r1): `request.ts` validates the
   resolved `[locale]` id against the §2 table and calls `notFound()`
   BEFORE any message file resolves (the static imports would otherwise
   throw a module error for an unknown id), and the layout derives `<html
   lang>` only after the same validation. A known-locale deep miss
   (`/de/<unknown>`) reaches the same boundary through the §1 catch-all,
   which calls `notFound()` inside `[locale]` so the locale-scoped
   not-found renders; an unknown-locale request answers its 404 from the
   root boundary — there is no localizing a locale that does not exist.
6. **Trailing slash:** Next default (`trailingSlash: false`) — `/de/` 308s
   to `/de`; canonicals are emitted in the answering form
   ([DR-2026-08-18-page-size] home-canonical law: declare the URL that
   answers 200).
7. **SSG:** `generateStaticParams` returns all nine ids; every
   layout/page calls `setRequestLocale` so each locale home prerenders
   statically. `<html lang>` takes the BCP-47 column, never the segment id.

Deep-path locale switching follows localization-architecture §4: the
switcher links the same page in the chosen locale when available, otherwise
that locale's section index. At F4 the only page is the locale root, so
every switch resolves; the law is implemented in `navigation.ts` now and
exercised by later pieces.

## 4. Chrome i18n — namespace, files ×9, stub-now/content-later

Separate namespace from game content, keyed per-locale resource files, EN
pivot, coverage-parity checked (localization-architecture §4). Files:
`site/messages/<id>.json` ×9, each a single `chrome` namespace object.
Game-content namespaces (overlays from `extracted/locales/*`, dig 14
wave-2) never enter these files — they arrive as pipeline artifacts and get
their own namespace/mechanism in the pieces that consume them.

**Initial key inventory (exactly 13; values are EN stubs at F4):**

```
chrome.nav.database · chrome.nav.map · chrome.nav.guides · chrome.nav.news ·
chrome.nav.tools · chrome.header.homeLabel · chrome.locale.triggerLabel ·
chrome.locale.listLabel · chrome.a11y.skipToContent · chrome.footer.label ·
chrome.footer.sections · chrome.notFound.title · chrome.notFound.body
```

(F5 owns its own keys — `search.*` enters with F5's commit, not before.
Anti-slop: no key exists whose only job is explaining the UI.)

**Stub policy, made mechanical.** `site/messages/_stub-keys.json` lists
every key still carrying pivot-EN stub text in the non-pivot files (all 13
at F4). Gates:

- `chrome-parity` test (§7): identical key set ×9, no empty values,
  identical `{placeholder}` sets per key, and every key in the stub ledger
  holds a byte-identical value across all nine files (so a "translated"
  value can't hide inside a stubbed key).
- **Launch gate:** `_stub-keys.json` must be empty before any non-localhost
  deploy — until then EN chrome on `/de` is a declared construction state,
  not a shippable locale page. Real chrome copy translates from the game's
  own localized UI vocabulary when dig 14's UI-string corpus lands; the
  game's own nouns stay in the game's mix (design-standard §5.4).

`request.ts` imports the nine files statically (deterministic SSG, no
dynamic-import waterfalls). Server components read via
`getTranslations("chrome")`; the provider passes `messages` for the one
client island (locale combobox).

## 5. Shell anatomy — NWG shell structure, wikily-grade finish, game chrome

All chrome rides the F1 canon: `.wt-panel` surfaces, `HeaderBg` band
(`--tex-header-bg`, slice 10, h35) with the −3px title offset, `ListHeader`
row bands, white-outline hover / white-glow selection recipes
(`--effect-hover-outline`, `--effect-selected-glow`), gold reserved to
currency/selection/focus (`--ring`). Serif-first: EB Garamond chrome, IM
Fell English wordmark. Text sizes only from the F1 ramp (≥12px floor,
design-standard §3).

**Header** (sticky, `banner` landmark, opaque `--bg-1` + bottom bevel
`--border`):

- Left: wordmark **WARTALES** (`--font-flavor`, `--text-display`) linking
  to `/` (bare pivot path from every locale — one product, one home).
- Nav row: section links rendered **only from
  `src/lib/routes-manifest.ts`** entries whose target route exists
  (`live: true`). Each entry carries `labelKey` — a chrome key from the §4
  inventory, no second vocabulary; hover = white outline + text brightens
  to `#FFFFFF`; active section = white glow. At F4 the manifest holds
  **exactly one live row**: `{ id: "home", href: "/",
  labelKey: "chrome.header.homeLabel", live: true }` — href resolved
  through `navigation.ts`, so it lands on the current locale's home
  canonical (`/` pivot, `/{locale}` prefixed; the URL that answers 200,
  [DR-2026-08-18-page-size]) (r1: one declared row, keyed from the §4
  thirteen, so §7.4's parity rule passes on the spec's own fixture and AC
  6's per-row link count is a deterministic boolean). Unbuilt sections are
  omitted, never captioned (copy-earns-its-place ¶2) — each landing piece
  flips its own row.
- **Search slot (F5's piece):** a reserved mount in the header grid,
  `data-shell-slot="search"`, rendering nothing at F4 — no input, no
  "Search" word, no disabled control (omitted, not captioned). Contract
  handed to F5: the mount occupies the nav row's right region; closed-state
  "word in the nav", open-state grow-across-the-row, 2-char trigger and
  in-place swap are F5's behaviors per
  [DR-2026-08-22-search-is-not-a-page]. There is no `/search` route and no
  search link anywhere in F4 output.
- **Main content wrapper — inbound F5 seam (r1):** the shell's `<main>`
  element, the wrapper holding page content between header and footer,
  server-renders **`data-search-swap-root`** on every page. This is the
  inbound half of the F5 contract —
  [`spec-f5-search.mdx`](./spec-f5-search.mdx) §5.1 ("the app shell puts
  that attribute on its main content wrapper") and its §13 H-3 ("must not
  be forgotten when F4 lands"). F4 places the attribute and NOTHING else
  of F5: no hidden-toggling, no results view, no matcher import — the swap
  machinery touches this element only from F5's side.
- Right: **locale combobox** — Radix Popover + Command upgraded per F1 §4:
  closed chip shows the current code (`DE`), list shows native names
  (§2 table), typed filtering accepted, gold `--ring` focus, selected row =
  white glow. Items are real `<a href>`s (server-renderable link graph,
  seo-standard §2) built with `navigation.ts` Link so the path is preserved
  and only the locale segment changes.
- First focusable element: skip link (`chrome.a11y.skipToContent`).
- Narrow viewport: nav collapses into a disclosure sheet — same chrome
  recipes, comfortable touch targets, 12px floor intact.

**Footer** (`contentinfo` landmark): same manifest-driven law — wordmark
repeat, section link columns (they deepen automatically when PT02's
taxonomy menu lands, NWG dbMenu pattern), no invented legal lines, no
absence copy. Single source: the same `routes-manifest.ts` feeds header nav
and footer, so the two can never disagree.

**Smoke home** (`[locale]/page.tsx`): wordmark lockup over `--bg-1`, nothing
else — no tagline, no data claims, no "coming soon" prose (anti-slop).
PT01 replaces this file wholesale in its own commit.

Landmarks: exactly one `banner`, one `contentinfo`, `navigation` on nav and
footer groups; the page `<h1>` belongs to pages, never to the shell.

## 6. Token wiring — Tailwind v4 theme over the frozen canon

`globals.css` (the only new stylesheet):

1. `@import "tailwindcss";` then `@import "./tokens.css";` (r1: globals.css
   sits beside tokens.css in `src/styles/`, so the import is
   sibling-relative — `./styles/tokens.css` would resolve to
   `src/styles/styles/` and break the first build) — the
   F1 file is consumed verbatim, never duplicated, never edited
   (tokens.test.ts byte-guards its literals; §7 keeps the suite green).
2. `@theme inline` maps the shadcn slots and game tokens to utilities:
   `--color-*` for every shadcn variable plus `--bg-1..4`, `--panel-well`,
   the text ramp, `--selection`, `--positive/--negative/--warning`,
   `--rarity-*`/`--light-rarity-*`, `--chart-1..5`, `--player-1..4`;
   `--font-ui/flavor/cjk/sans/serif/mono` for the font utilities, each
   right side a `var()` reference into the canon stacks — never a
   re-typed `"EB Garamond", …` string (the six theme names equal the six
   canon names, so which Tailwind-v4 spelling keeps tokens.css
   authoritative is a verify-at-install duty beside the §1 compatibility
   gate). **Aliasing law (r1):** every mapping whose right side
   names a canon value takes the aliasing form —
   `--color-bg-1: var(--bg-1)`, `--spacing-g1: var(--space-1)` — never a
   literal copy of that value (`--color-bg-1: #100F0E` would pass
   tokens.test.ts, which parses tokens.css alone, while creating exactly
   the second source of truth the never-touch law bans).
   **Literals (r1b, correcting N1):** there is no canon-undefined scalar
   to spend a literal on. The earlier example offered `--radius-*: 0` as
   one — FALSE: tokens.css L167–171 declares the whole family
   (`--radius`, `--radius-sm/md/lg/xl`, all `0rem`; tokens.test.ts pins
   each at `parseFloat == 0`), so globals.css declares NO `--radius-*` at
   all, and no shell/app source emits a `rounded-*` corner utility
   (square is F1 §2.4 canon — a rounded corner is a defect), which is why
   the omission costs nothing. Nor was the ban ever color-only: any
   literal right side — a hex/rgb/oklch/named color, a radius length, a
   font stack, a duration — whose left side matches a canon token name
   fails shell-hygiene (§7.5 r1/r1b), the failing command AC 9 names.
3. **Spacing law:** Tailwind's numeric scale is NOT remapped onto the
   game's 5px rhythm (silent conflation hazard). The game steps ship as
   named utilities `--spacing-g1..g8 ← var(--space-1..8)` (`p-g4`,
   `gap-g5`, …). Raw numeric spacing utilities (`p-4`, `gap-2`) are banned
   in shell/app components — the hygiene test greps for them (§7).
4. Base layer: `color-scheme: dark`; body = `--background`/`--foreground`;
   no `.dark` variant exists (`:root` IS dark, F1 canon; no light theme
   ships).
5. Component classes from tokens.css (`.wt-panel`, `.rarity-tier-*`) are
   used as-is; no second definition of any token or recipe anywhere in
   `site/src` outside tokens.css — hygiene-tested with `globals.css` in
   the sweep (§7.5 r1), so the claim holds as scoped.

## 7. Test spec (all node-env, beside or under `site/src`, discovered by the untouched `vitest run`)

1. **Existing canon stays green:** `tokens.test.ts` passes unmodified —
   including the served-refs guard, which keeps failing `npm test` while
   any declared font/texture binary is unstaged.
2. **`locale-routing.test.ts`:** the §2 table has exactly the 9 frozen
   client locales; `en` is the sole bare pivot; prefixes unique; BCP-47
   values valid (`pt-br`→`pt-BR`, `zh`→`zh-Hans`); native names non-empty;
   and exactly ONE row has `id !== clientCode` — `pt-br`→`pt-BR`, the
   §2(r1b) data-plane join (r1b).
3. **`chrome-parity.test.ts`:** the §4 gates (key-set ×9, non-empty,
   placeholder parity, stub-ledger consistency, all-13-listed-at-F4).
4. **`routes-manifest.test.ts`:** every `live: true` href resolves to an
   existing route file on disk; every manifest entry's `labelKey` names a
   chrome key present in all nine message files (r1: label keys come from
   the §4 inventory — at F4 exactly the home row's
   `chrome.header.homeLabel`). Dead nav links cannot ship.
5. **`shell-hygiene.test.ts`:** sweeps `src/components/**` +
   `src/app/**` **and `src/styles/globals.css`** (r1 — every file where a
   token declaration could land; tokens.css/tokens.test.ts excluded) for
   stock Tailwind palette classes (`slate|gray|zinc|neutral|stone-*-N`),
   raw numeric spacing utilities, `oklch(`, any canon-token name from
   tokens.css re-declared outside tokens.css, any `rounded-*` corner
   utility (r1b — square is canon), and — inside globals.css's `@theme`
   block — any literal value where the §6 aliasing law demands
   `var(--…)`, COLOR OR NON-COLOR (r1b): a radius length, a font stack, a
   duration — any non-`var()` right side whose left side matches a canon
   token name — plus any `--radius-*` declaration at all (§6 r1b
   omission). All must be absent; this suite is AC 9's failing command
   for both slices.
6. **`scripts/smoke.mjs`** (executable AC runner, exit-code verdict):
   boots `next dev` on a scratch port — a compile error in the boot log is
   itself a FAIL, so AC 1's "zero build errors" is detected here rather
   than asserted (r1) — then curls the §8 matrix. Full check list (r1,
   closing the AC 12/13 verification gaps): status codes for the nine
   roots; redirect Location + permanence; `lang` attributes; prefixed href
   correctness; wordmark/nav/footer presence; absence of `/en/` internal
   hrefs; the shell rendering no search input or search word beyond F5's
   mounted search-field host (r2b); `data-shell-slot="search"` present —
   empty until F5 mounts, then containing exactly the F5 search-field host
   and nothing else — with `data-search-swap-root` present on the main
   wrapper of every served page (AC 11); 404 legs — `/de/<unknown>` answers 404 with localized
   chrome and `<html lang="de">`, `/xx/foo` answers 404 with no locale
   redirect (AC 4, AC 14); the Noto split-subset stylesheet link PRESENT
   on `/zh`
   and `/ko`, ABSENT on the other seven, pattern per the fonts README
   (AC 13); every `/fonts/*.woff2` + `/assets/ui/*.png` ref found in
   served HTML curl-200 (AC 12 leg 1 — on machines with the binaries
   staged; unstaged machines print explicit `SKIP:` lines and the leg
   defers to the stage check below). Prints one PASS/FAIL/SKIP line per
   check. **Fresh-clone leg of AC 12:** `node scripts/stage-assets.mjs
   --check` lists every missing ref with the README fetch commands and
   exits nonzero before staging / zero after — that pair is its verifier;
   the dev-server-still-renders half holds because no ref blocks render
   (fallback stacks/bevels), asserted by the same smoke run on the
   unstyled-asset state.

## 8. Acceptance criteria

1. **Boots clean.** `npm run dev` serves on :3000 with zero build errors;
   `/` returns 200 pivot EN.
2. **Nine locale roots render:** `/`, `/fr`, `/de`, `/es`, `/pl`, `/pt-br`,
   `/ru`, `/ko`, `/zh` each return 200 (smoke).
3. **Pivot-prefix law:** `/en` and `/en/de` answer **301** to `/` and
   `/de` respectively (Location asserted); no served page contains an
   internal `href="/en..."` (smoke + grep of rendered HTML).
4. **No negotiation:** `/` with `Accept-Language: fr` returns 200 EN at
   `/`; `/xx/foo` returns 404 (never a locale redirect).
5. **Switch links resolve:** every locale-combobox item href on each served
   page returns 200 ×9, same-path (at F4: the locale root), correct prefix.
6. **Curl-greetable HTML** (FRAMEWORK §2.14): `/` and `/de` served HTML
   contain the wordmark, ≥1 nav `<a href>` per live manifest row, the
   footer landmark, and `<html lang>` = `en`/`de` respectively; no raw
   message keys (`chrome.`) leak into any served page.
7. **Vitest green:** full suite passes — `tokens.test.ts` unmodified and
   passing, plus the four new suites of §7.
8. **Chrome parity:** key sets identical ×9; placeholder parity; stub
   ledger lists exactly the stubbed keys and they are byte-identical across
   locales (§4 gates).
9. **Canon untouched (r1):** `git diff` empty for `tokens.css`,
   `tokens.test.ts`, `tokens-preview.html`, and `package.json`'s `test`
   script; no token re-declared outside tokens.css — enforced by
   shell-hygiene (§7.5), whose sweep includes `globals.css`, so any
   violation leaves a red command; and no canon VALUE duplicated either —
   the aliasing law covers non-color scalars (radius lengths, font
   stacks), globals.css declares no `--radius-*`, and no shell/app source
   uses a `rounded-*` utility (r1b).
10. **Hygiene laws hold:** zero stock-palette classes, zero raw numeric
    spacing utilities, zero `oklch(` in shell/app sources (test-enforced).
11. **Search slot honored, swap root placed (r1, r2b):**
    `data-shell-slot="search"` present in served header HTML — empty until
    F5 mounts, then containing exactly the F5 search-field host and
    nothing else (staged behavior: green at F4-only time AND after F5
    lands); `data-search-swap-root` present on the served main content
    wrapper of every page (smoke) — F5's inbound seam, spec-f5-search
    §5.1 + §13 H-3; the shell itself renders no search input or "Search"
    nav word beyond F5's mounted host, and there is no `/search` route or
    link anywhere.
12. **Assets resolve:** every `/fonts/*.woff2` and `/assets/ui/*.png` ref
    reachable (curl 200) on a staged machine; on a fresh clone the dev
    server still renders (bevel/stack fallbacks) and the missing-ref state
    is reported by `npm run stage` with the README commands.
13. **CJK routing discipline:** the `/zh` and `/ko` documents emit the
    Noto split-subset stylesheet link (fonts README pattern); the other
    seven locales emit none.
14. **Production build (r1):** `next build` succeeds; all nine locale
    homes prerender as static HTML. The locale-scoped not-found becomes a
    real served artifact through the §1 `[...rest]` catch-all calling
    `notFound()` inside `[locale]` — the mechanism that makes the nested
    `not-found.tsx` fire, since a nested not-found file alone never
    matches a URL — and smoke asserts the result: `/de/<unknown>` returns
    404 carrying localized chrome and `<html lang="de">`.

## 9. Files touched

**Created:** `site/next.config.ts`,
`site/tsconfig.json`, `site/postcss.config.mjs`,
`site/src/middleware.ts`, `site/src/app/[locale]/layout.tsx`,
`site/src/app/[locale]/page.tsx`,
`site/src/app/[locale]/[...rest]/page.tsx` (B1 r1 — the catch-all that
fires the locale-scoped not-found),
`site/src/app/[locale]/not-found.tsx`,
`site/src/i18n/{locales,routing,request,navigation}.ts`,
`site/src/lib/routes-manifest.ts`,
`site/src/components/shell/{header,nav-row,locale-combobox,search-slot,footer}.tsx`,
`site/src/components/ui/{button,popover,command}/…` (shadcn copies, upgraded),
`site/components.json`, `site/src/styles/globals.css`,
`site/messages/{en,fr,de,es,pl,pt-br,ru,ko,zh}.json` (×9),
`site/messages/_stub-keys.json`, `site/scripts/{smoke,stage-assets}.mjs`.

**Modified:** `site/package.json` (deps + dev/build/start/stage/smoke
scripts; `test` untouched); `site/.gitignore` (r1 — EXTENDS the as-found
rules `node_modules/`, `package-lock.json`, `public/fonts/*.woff2` with
`.next/` and `next-env.d.ts`; nothing removed — overwriting would drop
three live ignore rules). **Never touched:** `site/src/styles/tokens.css`,
`site/src/styles/tokens.test.ts`, `site/src/styles/tokens-preview.html`,
anything under `site/public/` (written only by the stage script, binaries
stay uncommitted per the README conventions), anything under `extracted/`.

**Seam reconciliations (r1):**

- **Route registry filename.** F4 ships
  `site/src/lib/routes-manifest.ts`; this confirms the name for
  [`spec-pt16-seo.mdx`](./spec-pt16-seo.mdx) §3 ("exact filename is F4's
  to confirm"). PT16 generators import locales/routes from that module;
  no `routes.ts` exists or will.
- **Locale segment casing (r1b — errata resolved).** The PT16-arbiter
  collision adjudication
  ([`arbiter-pt16-seo.mdx`](./arbiter-pt16-seo.mdx)) settles ON THIS
  SIDE: the lowercase `pt-br` page segment stands
  ([DR-2026-08-20-locale-urls] fixes membership + the bare-path law, not
  case), superseding PT16 §3.2's provisional `/pt-BR/` page expectation;
  hreflang keeps BCP-47, so PT16 output is unaffected. The data half of
  the same ruling binds here too: page paths never compose `/data/**`
  URLs by string surgery — the stripped segment joins through §2's
  `client code` column, and `api.manifest.json` carries the additive
  `locales.pageSegments` bridge (`{"pt-br": "pt-BR"}`, §2 r1b) that F2
  §6's formula needs; F2 itself is untouched.
- **Search swap root.** F4 places `data-search-swap-root` on `<main>` —
  closing [`spec-f5-search.mdx`](./spec-f5-search.mdx) §13 H-3 from this
  side; attribute placement only, per §5.

Relinks: [pieces.mdx](./pieces.mdx) F4/F5 rows ·
[spec-f5-search.mdx](./spec-f5-search.mdx) §5.1, §13 (slot consumer +
swap-root seam) ·
[spec-pt16-seo.mdx](./spec-pt16-seo.mdx) §3 (route registry consumer) ·
[spec-tokens.mdx](../design/spec-tokens.mdx) (F1 canon + component canon §4) ·
[shadcn-adoption.mdx](../design/shadcn-adoption.mdx) ·
[locale-key-convention.mdx](./locale-key-convention.mdx) ·
[fonts README](../site/public/fonts/README.md) ·
[textures README](../site/public/assets/ui/README.md) ·
[`_foundation/localization-architecture.md`](../../_foundation/localization-architecture.md)
§1–§5 · [`_foundation/design-standard.md`](../../_foundation/design-standard.md)
§2, §3, §5 · [`_foundation/nw-site-new-deep-dive.md`](../../_foundation/nw-site-new-deep-dive.md)
§5, §9 · [`_foundation/decision-register.md`](../../_foundation/decision-register.md)
[DR-2026-08-24-wartales-roster], [DR-2026-08-20-locale-urls],
[DR-2026-08-22-search-is-not-a-page],
[DR-2026-08-22-copy-earns-its-place],
[DR-2026-08-18-page-size] ·
[`FRAMEWORK.md`](../../FRAMEWORK.md) §2.7, §2.11, §2.14, §2.15, §2.20 ·
[`spec.md`](../spec.md) `locales` block · [F5 brief](./briefs/documentator-f5-search.mdx)
(slot consumer).

## Revision r1

Fresh Spec Fixer agent, 2026-08-26 — all twelve findings of
[`review-spec-f4-shell.mdx`](./review-spec-f4-shell.mdx) resolved in this
file only. AC set stays at **14** with stable ids; amended bodies are
tagged `(r1)`; no AC renumbered, none argued away.

| id | sev | resolution | sections touched |
|---|---|---|---|
| B1 | BLOCKER | `[...rest]` catch-all calling `notFound()` added so the nested locale not-found actually fires; file set + AC 14 amended | §1 tree, §8 AC 14, §9 |
| M1 | MAJOR | `data-search-swap-root` placed on `<main>` per spec-f5-search §5.1/H-3 — attribute only; AC 11 extends | §5, §7.6, §8 AC 11, §9 |
| M2 | MAJOR | manifest = exactly one live home row keyed `chrome.header.homeLabel`; §7 rule = labelKey parity ×9 from §4 inventory | §5, §7 item 4 |
| M3 | MAJOR | hygiene sweep includes `globals.css`; `@theme inline` aliasing law bans literal copies; AC 9 names its failing command | §6, §7 item 5, §8 AC 9 |
| M4 | MAJOR | smoke enumerates every AC leg incl. Noto ×9, asset curls, 404 legs, boot cleanliness; `stage-assets --check` owns the fresh-clone leg | §1 scripts note, §7 item 6 |
| m1 | MINOR | home-canonical law recited to [DR-2026-08-18-page-size]; relink list corrected | §3 rule 6, §9 |
| m2 | MINOR | import fixed to sibling `./tokens.css` (globals.css lives beside it) | §6 item 1 |
| m3 | MINOR | as-found `.gitignore` extended not created; §0 scaffold claim scoped to truth | §0, §9 |
| m4 | MINOR | `routes-manifest.ts` filename confirmed to PT16 §3; PT16 relink recorded | §9 |
| m5 | MINOR | request.ts/layout validate the locale id and `notFound()` before message resolution; guard stated as load-bearing | §1 tree, §3 rule 5 |
| m6 | MINOR | install-time peer-resolution + smoke gate over next-intl API surface; named fallback recorded | §1 |
| m7 | MINOR | `pt-br` casing promoted to a recorded decision with the F2 client-code data-plane boundary stated | §2, §9 |

Boundary fences intact after revision: PT01 (smoke home still replaced
wholesale), PT02 (footer deepening deferred), PT16 (hreflang/sitemap/JSON-LD
still excluded — only the filename/casing reconciliation is recorded),
PT17 (404 mechanism only, unbranded), F5 (outbound slot-only fence kept;
the new inbound seam ships an attribute, never behavior).

## Revision r1b (errata)

Fresh Spec Fixer agent, 2026-08-26 — two prescriptions only, this file
only, nothing committed, no subagents. AC set stays at **14** with stable
ids; the touched body is tagged `(r1b)`; none renumbered, none argued
away. The locale table is self-consistent end-to-end after this revision:
every example names its plane — page examples spell the `id` column
(`/pt-br/…`, routing array, AC 2 roots, `pt-br.json` messages), data-plane
examples spell the `client code` column (`/data/pt-BR/…`).

| # | source | prescription | resolution | sections touched |
|---|---|---|---|---|
| 1 | [`arbiter-pt16-seo.mdx`](./arbiter-pt16-seo.mdx) Adjudication (binding errata; primary fix = F4 side) | exercise the §2 reserved locale-table override so the PAGE segment stays lowercase `pt-br` ([DR-2026-08-20-locale-urls] unchanged) while the DATA plane composes `/data/pt-BR/…` via the client code | override recorded as EXERCISED on the existing row (`pt-br` → clientCode `pt-BR`, cells unchanged — the divergence is the point); new composition law: page→data joins resolve the stripped segment through the table, never page-path string surgery (verbatim F2 §6 composition of `/pt-br/item/Gold` would emit `/data/pt-br/…`, outside F2's declared set); stated precisely what `api.manifest.json` must carry — additive `locales.pageSegments: {"pt-br": "pt-BR"}` with a three-way resolution rule — because F4's table alone cannot guarantee F2 §6 composes (`prefixed` is frozen to client codes by F2 §15 AC8); F2 untouched; routing array annotated page-plane; §7.2 pins the single divergent join as test law | §2, §3, §7 item 2, §9 seam |
| 2 | [`arbiter-f4-shell.mdx`](./arbiter-f4-shell.mdx) finding N1 (MINOR, non-gating) | correct the false carve-out claim; extend the hygiene grep to non-color canon values with a failing command | carve-out deleted: tokens.css L167–171 DOES define `--radius/--radius-sm/md/lg/xl` (all `0rem`; tokens.test.ts pins each at 0), so no canon-undefined scalar exists and the old "scalars the canon does not define (`--radius-*: 0`)" sentence was false; globals.css now declares NO `--radius-*` at all and `rounded-*` utilities are banned (square is F1 §2.4 canon); aliasing law extended beyond colors — radius lengths, font stacks, durations: any non-`var()` right side whose left side matches a canon token name fails shell-hygiene (§7 item 5), which remains AC 9's failing command for both slices; font mappings pinned to `var()` right sides with the name-collision spelling left as a verify-at-install duty beside the §1 gate | §6, §7 item 5, §8 AC 9 |
| r2b | [`arbiter-f5-search.mdx`](./arbiter-f5-search.mdx) §3 adjudication (binding cross-spec errata, routed as E-2) | re-stage the two search-slot legs whose assertions become false the day F5 mounts — smoke.mjs is a standing runner (`npm run smoke`), so an unqualified emptiness leg goes permanently red post-F5 and AC-11 becomes unverifiable-green, i.e. unfalsifiable as an exactness claim | slot leg restated: `data-shell-slot="search"` present; EMPTY until F5 mounts, then containing exactly the F5 search-field host and nothing else; absence legs rescoped from global absence to ownership — the shell renders no search input or "Search" nav word beyond F5's mounted host (post-mount the closed-state chrome word renders in served HTML from F5's side, which killed the old absolute bans); swap-root leg and `/search`-route/link absence untouched — both survive F5 unchanged; every amended leg stays green at F4-only time AND after F5 lands, zero code-level conditionals because F4 has not built yet (`site/scripts/`, `app/**` absent on disk) | §7 item 6, §8 AC 11 |

Boundary fences intact after r1b: PT01 wholesale home replacement · PT02
footer deepening deferred · PT16 hreflang/sitemap/JSON-LD still excluded
(the casing seam now records the RESOLVED arbitration instead of a
provisional freeze, plus the manifest-mediated data-plane join) · PT17
mechanism-only · F5 outbound slot-only / inbound attribute-only.

Ledger continues with r2b above (heading kept so the
[Revision r1b](#revision-r1b-errata) anchor stays valid; r1b rows stand
byte-intact). Boundary fences after r2b: unchanged from r1b — F4 still
ships only the slot mount and the swap-root attribute; the amendment
rewords what the standing checks ASSERT across the F5 mount, never what
F4 builds.
