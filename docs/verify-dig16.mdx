# Verifier — Dig 16 canonical wiring

**Verdict: PASS** — all eight brief items pass; zero failing item ids.

Fresh Verifier agent, 2026-08-26. Subject: the Dig 16 R10b canonical-wiring
claim set ([data-dig-log.mdx](data-dig-log.mdx) §"Dig 16"). No subagents;
no commits; writes confined to this file plus
`output/_verify-dig16/`. Every probe below ran as an independent
recomputation — fresh hashes, fresh parses, fresh arithmetic — never a
re-read of the dig agent's conclusions.

## State verified against (important)

The pack tree moved **during** this verification: a concurrent Dig 17
(deferred-kind carriers, rider R6) added 50 `_logic/` files at 11:56,
amended `pipeline/tools/relink_catalog.py` at 12:19, and re-emitted the
catalog plane twice (`matrix.json` sha256 `534c30c38bda712d…` →
`a40bcc9f5e0e1d15…` → `f83f4d931f3b86d9…`). Dig 16's own artifact state was
therefore pinned first and every Dig-16 verdict below is computed against
those pinned bytes:

| artifact | sha256 (prefix) | status at dig-16 close |
|---|---|---|
| `extracted/relinks/matrix.json` | `534c30c38bda712d` | observed shipping at verification start; preserved in `output/_dig-canonical-wiring/matrix.{dry,dry2,post-check}.json` |
| `extracted/RELATIONS.md` | `995d14436895edc5` | same (`.dry`, `.dry2`, `.post-check`) |
| `relinks.pre.manifest.json` | 163 entries | all 163 files byte-identical today except `matrix.json` |

Current-tree consumer checks (item 8) additionally re-ran against the
newest post-Dig-17 state and pass there too.

## Mechanical axis

### M1 — Pair-file integrity: PASS

Independent sha256 sweep of all 163 manifest-listed relinks-tree files vs
`output/_dig-canonical-wiring/snapshots/relinks.pre.manifest.json`
(`v1_integrity.txt`):

```
manifest entries: 163
missing (in manifest, absent on disk): []
CHANGED vs pre-wiring snapshot: ['matrix.json']
top-level pair files: 51 (EXPECTED_PAIR_FILES = 51 )
extracted/relinks/matrix.json: CHANGED (cur 534c30c38bda712d, pre 5efaba53b05bf2ff)
```

Exactly ONE manifest file changed — the catalog itself. All 51 hard-modeled
pair files, every `_logic/` row byte, and both non-pair jsonl sidecars are
byte-identical to pre-wiring; the frozen `EXPECTED_PAIR_FILES = 51`
gate (`wave_kinds.py`) holds. The only disk additions are 50 Dig-17 files
(mtimes 11:56+, absent from the pre-wiring manifest) — not Dig-16 fallout.
`locale_availability.jsonl`, regenerated by a stage-6 child at 12:01, still
hashes identical to its pre-wiring bytes — free extra determinism evidence.

### M2 — Envelope arithmetic: PASS

Every additive counter recomputed from `_logic/` + canonical pair files by
fresh code (`v2_envelope.py`, `v2_envelope.txt`):

```
[ok] evidenceFiles        recomputed=107   (claimed 107)
[ok] evidenceEdges        recomputed=7184  (claimed 7184)
[ok] evidenceMethods      {'cdb-payload-id-join': 5942, 'hscript-enum-ref': 1242}
[ok] derivedDirections    89   [ok] foldedModeledDirections 8
[ok] foldedSelfPairKinds  1    [ok] selfChainFamilies     9
[ok] ledgeredDirections   1432 [ok] ledgerClasses         {9 classes, exact}
[ok] totals.files=51 edges=19130 modeled=39 ordered=1560 selfPairs=9 deferredTargetFiles=3
[ok] in JSON: 39+89+1432=1560      [ok] on disk: 39+89+1432=1560
[ok] derived/fold/self-chain edge split 6014/139/1031 reconciles to 7184
[ok] ledgered.sha256 pin == sha256(_ledger.jsonl)
[ok] all 107 derivedEvidence.directions pins (sha/edges/mechs)
[ok] per-direction evidence blocks consistent with unblock text
```

Method split cross-checked raw: `hscript-enum-ref:{Attribute 7, Item 7,
Skill 148, Status 996, Trait 5, UnitClass 79}` sums to 1,242. Closure
identity 39+89+1,432=1,560 holds in the JSON AND independently on disk.

### M3 — Locator truthfulness: PASS

14 locators sampled deterministically across families (`v3_locators.py`,
`v3_locators.txt`): 12 wired directions stratified over the 89 (largest
family `skill→status` E=664 and two `logic`-mechanism directions forced
in), plus one modeled-direction fold and the `region↔region` fold-self.
For each: family exists, sha256 == catalog pin (both in
`pairs[].evidence` and `derivedEvidence.directions`), row count == E,
mechanism set == `mech=`. Then 16 individual edges re-derived from their
cited draft locators on fresh parses:

```
[skill→status]        edge MaintainingTheLine->Pushback [cdb-payload-id-join]: PASS - ok
[skill→status]        edge LightHelmetCraftT3->Blind [hscript-enum-ref]: PASS - ok (exact-offset; target in canonical status ids)
[skill→attribute]     edge Idee41->DamageBonusPercent [hscript-enum-ref]: PASS - ok (exact-offset ...)
[confessions→item]    edge ConfessionHate->ActionPoint ... nested data[].choices[].gains.item[].inf: PASS
... 14/14 direction verdicts PASS; V3 VERDICT: PASS
```

Both hscript probes hit the cited token at the EXACT byte offset inside the
draft row's `script`; every `toId` resolves into the canonical target-kind
id set; payload values match verbatim through nested list paths.

### M4 — validate_all: PASS 8/8

Ran in a hash-proven scratch mirror (validate_all's availability child
rewrites `extracted/locales` + `locale_availability.jsonl`; the live tree
must stay read-only). Two legs, both green (`v5_validate.txt`):

Leg B — dig-16-state reconstruction (dig-16's own `matrix.post-check.json`
/`RELATIONS.post-check.md` bytes + manifest-pinned `_logic/`):

```
== validate_all [B_dig16state] == exit 0
validation: PASS (8/8 checks)
  [ok] verifier-gates: exit 0, allChecksPassed=True
  [ok] census-closure: 10207 + 1266 == 11473
  [ok] census-per-kind: 0 deviations
  [ok] relink-integrity: pairFiles=51; edges=19130; valid:false=0; valid:null=72 (61+9+2); non-hard=0
  [ok] availability-regenerated: rows=14456 over 34 kinds ...; fillerCensus=3 ...
  [ok] catalog-parity: pairCount=780; uncovered=0; markerMatch=True; mdMatrixRows=780; cellMismatches=0
  [ok] deferred-present: [...] (frozenMatch=True)
  [ok] hbson-independent: banned=absent; nonManaged=3 -> [... '_logic\\_ledger.jsonl']
```

Leg A — then-current tree (incl. Dig-17 files): identical `PASS (8/8)`.
Mirror-vs-tree hash mismatches: 0 on both legs.

### M5 — Emitter byte-stability: PASS (strongest form)

Three independent proofs:

1. Dig-era runs agree four ways: `matrix.{dry,dry2,post-check}.json` all
   `534c30c38bda712d` (emitted 11:22:50 / 11:24:33 / 11:26:01) ==
   shipped-at-start matrix.json; same 4-way identity for RELATIONS.md at
   `995d14436895edc5`.
2. Bytecode replay: the stale
   `pipeline/tools/__pycache__/relink_catalog.cpython-314.pyc` (compiled
   11:22:49) header-records source mtime 11:22:34 and size **37,801 B** —
   exactly the dig-log's tool pin (`af62b28e…`, 37,801 B). Executing that
   dig-16-era bytecode against my independently rebuilt dig-16 input mirror
   (current canonical bytes minus the 50 Dig-17 additions; 0 mirror hash
   mismatches) reproduces BOTH shipped artifacts byte-for-byte:

```
wrote ...\pycreplay\matrix.json (sha256 534c30c38bda712d...) + ...\RELATIONS.md
replayed matrix.json  == dig-16 marker 534c30c38bda712d: True
  == byte-equal to dig scratch matrix.dry.json: True
replayed RELATIONS.md == dig-16 marker 995d14436895edc5: True
  == byte-equal to dig scratch RELATIONS.dry.md: True
```

3. Structural diff of today's amended emitter over the same dig-16 inputs:
   ONLY additive Dig-17 keys differ (`deferredOutbound{}`,
   `totals.deferredOutbound*`, extended provenance string);
   `pairs/selfPairs/deferredTargetPairs/derivedEvidence/ledgered/
   textKeyCoverage` and all original totals are value-identical.
   Determinism on the then-current tree: two fresh runs byte-equal to each
   other and to the then-shipped `a40bcc9f` / `2a88bd80`.

## Adversarial axis

### A6 — Ledger leakage: PASS

Partition reconstructed independently from disk + emitted JSON
(`v6_leakage.py`, `v6_leakage.txt`):

```
disk truth: modeled=39 derived=89 ledgered=1432 of 1560 ordered directions
[ok] P1a/b/c modeled, derived, ledgered pairwise disjoint
[ok] P2 union == 1560 ordered directions (no missing, no extra)
[ok] P3 all 1432 ledgered directions render status partial|missing +
     unblock 'ledgered (dig 15): <class>' + matching ledgerClass, NO evidence block
[ok] P4a no canonical pair FILE typed toward a ledgered direction
[ok] P4b every populated pair file is typed modeled
[ok] P5  no _logic family file names a ledgered direction
[ok] P6a derivedEvidence carries no ledgered direction
[ok] P6b no direction carries both evidence and ledgerClass
[ok] P6c matrix evidence directions == disk-derived directions (89==89)
V6 VERDICT: PASS
```

The emit-time partition check's claim is confirmed independently: no
ledgered-class direction appears as an edge anywhere in the canonical
planes.

### A7 — Overclaim scan: PASS (with recorded notes)

- RELATIONS.md upgraded cells/sections state exactly what the counters
  support: "107 families · 7,184 edges · 89 derived + 9 folded · 1432
  ledgered" (all recomputed, M2); "WITHOUT moving a canonical pair file"
  (proven, M1); "the ledgered residue stays out of every edge set"
  (proven, A6); closure printed as arithmetic that verifies.
- PROOF.md R10's "DR bars 1–2 closed…" is scoped ("for everything
  enumerable at buildid 20318128") and its WIRED-by-Dig-16 clause matches
  artifact reality point-for-point (89 wired unblocks, 8 evidenceFolds,
  1,432-row `ledgered{}`, 107-family `derivedEvidence{}` with method split,
  byte-unchanged modeled plane). Its "parity marker now `534c30c3…`"
  sentence is scoped to the Dig-16 clause — historically accurate; the
  CURRENT marker differs only because Dig 17 later re-emitted (its own
  logged clause covers that).
- Cited verification chain exists:
  [verify-dig15-a.mdx](verify-dig15-a.mdx) "Verdict: PASS",
  [verify-dig15-b.mdx](verify-dig15-b.mdx) "VERDICT: SOUND",
  [verify-dig15-delta.mdx](verify-dig15-delta.mdx) "Verdict: PASS".
- Open debt is enumerated, not buried: name-hit adjudication (60), bar-2
  windows (126), trade-price-matrix gap, patch-diff recheck line.
- Owed follow-up stated honestly in the dig-log: spec-stages-datasets §4.2
  still describes the PRE-amendment envelope pending its decision-register
  entry + re-freeze — recorded as owed, nowhere claimed as done. No
  "closed/wired" language beyond what the counters support anywhere in the
  two upgraded artifacts.

### A8 — Consumer safety: PASS

cdb_verify invoked directly against the LIVE tree (read-only), both before
and after the last Dig-17 re-emit:

```
datasets verified : 40 files, 10207 rows (census sum over their sheets: 10207)
relink files      : 51, edges 19130 (expected 19130), dangling 0,
                    unverifiable 72, valid-flag mismatches 0
ALL CHECKS PASSED
```

Stage test file `pipeline/tests/test_stages_datasets.py` (the catalog /
stage-5/6 consumer suite), caches disabled:

```
36 passed in 16.26s          (against the 12:19-era tree)
36 passed in 39.56s          (re-run against the newest 12:36 tree)
```

Plus validate_all `catalog-parity` markerMatch=True on the new
`534c30c3…` marker (M4 leg B) and on the successor markers (leg A).

## Findings (non-blocking)

1. **Concurrent-Dig-17 churn during verification (environment, not a
   Dig-16 defect).** Between 11:56 and 12:37 the sibling dig added 50
   `_logic/` files, amended `relink_catalog.py` (now 44,556 B), and
   re-emitted `matrix.json`/`RELATIONS.md` twice (`a40bcc9f…` →
   `f83f4d93…`), landing its own
   [data-dig-log.mdx](data-dig-log.mdx) entry. All Dig-16 verdicts above
   are therefore anchored to hash-pinned dig-16 bytes; the current-tree
   legs were additionally re-run late and pass. Consequence to record: the
   Dig-16 emitter SOURCE bytes no longer exist on disk (pin `af62b28e…`
   corroborated indirectly via the pyc header and behaviorally via the
   byte-exact replay in M5).
2. The full-suite pytest errors quoted in the dig-log (T10 fault-injection
   ×8) sit outside the file chosen here and are pre-existing rider-R2
   re-freeze debt per the dig-log's own accounting; nothing in this
   verification contradicts that attribution.

Scratch: `output/_verify-dig16/` (`v1_integrity` … `v6_leakage` scripts +
outputs, `v7_final_sweep.py`, `mirror/`, `mirror_A_current/`,
`mirror_B_dig16state/`, `run1/`, `run2/`, `cur1/`, `cur2/`, `pycreplay/`,
`validate_A_current/`, `validate_B_dig16state/`, `pytest_stages_datasets.txt`,
`cdb_verify_live.txt`).
