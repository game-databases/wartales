# Brief — Drafter: extracted/PROOF.md (fresh subagent)

Fresh data-layer agent. Never spawn subagents, never commit. Wartales
pack only. Read root AGENTS.md; extraction-doctrine "Principle two"
(what PROOF.md must contain) IN FULL; then assemble from REAL
artifacts only: `docs/client-recon.mdx` (source inventory + measured
totals + methods), `extracted/harvest/harvest-report.json` (full-run
numbers), `extracted/harvest/*.summary.json`, `docs/cdb-census.mdx`
(386 sheets, row totals), `docs/data-dig-log.mdx` (closure proof
11,473 = census total), `extracted/logic/hl-structure/WALK-PROOF.mdx`
(zero-slack bytecode), `docs/decompile-dig-1.mdx`, `docs/toolchain-validation.mdx`.

Deliverable: `extracted/PROOF.md` per the doctrine's 4 sections:
1. Source inventory — every container (4 paks + res/ + hlboot.dat +
   Steam manifest) with sizes/totals + HOW measured.
2. Coverage reconciliation — per source: extracted counts vs source
   totals, coverage %, diffs explained (harvest 40,201/40,201; CDB
   closure 11,473/11,473; bytecode zero-slack; locale bridge
   55,350/55,350).
3. Residue ledger — everything NOT yet extracted, each with reason +
   concrete unblock: HBSON prefab decode (deferred kinds: place/
   group/element/levelProps), achievements (Steam surface), full
   decompiler (codegen from structure tables), audio offload (Wwise
   2.49 GiB still in place pending D:\ offload), fr-export vintage
   anomaly (patch-diff carrier gap).
4. Remaining theoretical surface — what else could be extracted
   (save-format parsing, co-op protocol inventory per doctrine's
   protocol section, steam depot diffing for patch pipeline) with
   fight-for-it recommendations.
HONESTY RULE: every number cites its artifact file; nothing invented;
open items stay open. Return: ≤12-line summary — coverage headline +
ledger size.
