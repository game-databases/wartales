# Brief — TestFixer: pipeline/tests/test_harvest.py round 1 (fresh subagent)

Fresh TestFixer. Never spawn subagents, never commit. Wartales pack
only. Read `docs/arbiter-harvest-build-r1.mdx` in full, then execute
ONLY its "pipeline/tests/test_harvest.py — FIX" ordered list,
verbatim, in order. CodeFixer has already landed its 10 items (suite
currently 35 passed / 0 failed / 3 skipped). Change nothing in
`pipeline/harvest.py`. After fixing: `python -m pytest pipeline/tests -q`
must meet the arbiter's stated baseline; include the exact counts in
your summary. Return: ≤10-line summary — per-item ✓/✗ + final counts.
