# Brief — Fixer: spec-harvest.mdx revision per arbiter fix list (fresh subagent)

Fresh Fixer. Never spawn subagents, never commit. Wartales pack only.
Read `docs/arbiter-specs.mdx` in full, then execute ONLY its
"spec-harvest.mdx — REVISE" ordered fix list, verbatim, in order, no
reinterpretation — plus arbiter-added finding A1 (byte-layout
boundaries 12 vs 16 + round-trip unit assertion requirement). The
arbiter ruled; do not re-litigate, do not add scope. Keep every other
spec-harvest.mdx line byte-identical. Return: ≤8-line summary — each
fix done ✓/✗ with one clause.
