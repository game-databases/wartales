# Brief — CodeWriter: F5 header search (Batch 3)

Fresh CodeWriter agent; never spawn subagents; never commit; Wartales
pack only. Build `docs/spec-f5-search.mdx` EXACTLY — frozen
build-ready (r1 + arbiter narrow + r2 all applied; ko prefix law
executed-green in-spec).

Read IN FULL: the spec; the F4 shell you build INTO
(`site/src/components/shell/header.tsx`, `search-slot.tsx`, `main`
wrapper's `data-search-swap-root` — read-only for the slot contract,
edit ONLY where §11 assigns); `docs/spec-f2-contracts.mdx` §6 (URL law
for hrefs); FRAMEWORK §2.9; DR-2026-08-22 verbatim table; F1 token
canon (never-touch); existing suites (your work must keep them green).

Build laws:
- §11 files-touched exhaustive: 14 new + 3 edited; a blind sibling
  TestWriter owns new test files — create none.
- Index artifact: emit-stage step-3 emitter under pipeline/tools +
  run_all wiring per spec §3, generating real
  `site/public/data/search/{locale}.json` ×9 + manifest from extracted/
  canonical names + availability rows — recount-governed, byte-
  deterministic, no magic constants.
- searchRows: pure module, server+client import rules, normalization
  tiers exactly §4.3 incl. the step-7 ł→l fold and ko jamo prefix law.
- Header field state machine: DR-verbatim behavior table, hidden-never-
  unmounted swap rooted at `[data-search-swap-root]`, 2-char trigger,
  50-row cap, anti-slop zero state, no form/submit, Escape/clear
  restore.
- No /search route anywhere (§6 sweeps); chrome merge mechanics §8/m5
  (stub-ledger placeholder parity then resolver deletion).
- Verify before finishing: `npm test` green INCLUDING all prior suites;
  index artifact regenerates byte-identical twice; live smoke with
  WARTALES_SMOKE_LIVE=1 still PASS; quote outputs. df-check before
  heavy steps (C: shared, has recovered but watch it).

Final message: files vs §11, verification outputs, deviations (none
expected).
