# Brief — CodeFixer: map pipeline round 1 (fresh subagent)

Fresh CodeFixer. Never spawn subagents, never commit. Wartales pack
only. Read `docs/arbiter-map-build-r1.mdx` in full — execute BOTH its
ordered fix lists (map_tiles.py items 1..N AND test_map_tiles.py
items), verbatim, in order, including the arbiter's modifications
(M3 group-hash approach, Mo1 known-affine absolute targets, Mo3
yAxis registry wording) and its two new findings (canonical served-
tile URL shape pinned on BOTH sides same-commit; AC7 validates a key
nobody ships — align to the shipped registry). After: full suite
green (the arbiter names the expected skip-to-green conversion for
the 21 behavioral tests). Return ≤10-line summary — per-item ✓ +
final counts.
