# Brief — Documentator: wartales/tools-plan.md (fresh subagent)

You are a FRESH Documentator subagent launched by the wartales
orchestrator. You never spawn other subagents. You never commit to git.
Wartales pack only.

## Read first

1. `C:\_reps\game-databases\AGENTS.md` — in full.
2. `C:\_reps\game-databases\_foundation\site-sections.md` — the
   tool-discovery process (your method, step by step).
3. `C:\_reps\game-databases\wartales\docs\competitor-research.mdx` — in
   full (your primary evidence: §2 inventory, §4 pain points, §6 scoring
   table, §7 applied delta).
4. `C:\_reps\game-databases\wartales\docs\client-recon.mdx` — §7 game
   brain (data.cdb, formulas.hx, hscript) = what the logic layer can
   power.
5. `C:\_reps\game-databases\FRAMEWORK.md` §2.16 (tools are plugins;
   flagship = launch-scope when research says market gap).

## Deliverable

`C:\_reps\game-databases\wartales\tools-plan.md`:

1. **Competitor tool inventory** — every tool found, with traffic
   estimates and the evidence basis (from competitor-research §2/§6).
2. **Mechanics enumeration** — every numerical system the game computes,
   from recon evidence (formulas.hx, data.cdb sheets, hscript sources)
   + competitor relationship model: trade price gradients, wages/upkeep,
   food consumption, XP curves, capture/tame odds, arena betting odds,
   injury/death, morale, adaptive difficulty, crafting quality tiers,
   ghost curses, path perks. Mark each: confirmed-in-client vs
   community-inferred vs P0-DIG.
3. **Scored tool list** — ≥8 candidates (floor is 5), each scored
   traffic-potential × build-cost × data-readiness × defensibility with
   the scoring VISIBLE. Moat tools first: things competitors cannot
   build because only full deconstruction gives the numbers (exact
   capture weights, price caps, adaptive-difficulty params). The
   company/troop builder is the flagship (promptForDB names build
   creation the top tool) — score it accordingly and spec its MVP
   scope.
4. **Ship order** — MVP fast → result states in shareable URLs →
   embedded into related entity pages (interlinking rule).
5. **Evidence links** — every candidate cites its pain-point row or
   competitor gap.

Facts only; no legality commentary. Return: ≤12-line summary — top 5
by score and the flagship MVP scope in one sentence.
