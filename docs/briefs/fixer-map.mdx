# Brief — Fixer: docs/spec-map-pipeline.mdx (fresh subagent)

Fresh Fixer. Never spawn subagents, never commit. Wartales pack only.
Read `docs/review-spec-map.mdx` in full, then fold EVERY finding
(F1–F13 + the addendum's two-item payload-vocabulary extension)
into `docs/spec-map-pipeline.mdx` — the review says the answers are
already extracted; this is an editing pass, no re-scouting. Where a
fix needs a number, take it from the cited artifact (TSV, census,
recon). Change nothing else. Return: ≤8-line summary — per-finding ✓.
