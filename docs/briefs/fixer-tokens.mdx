# Brief — Fixer: design/spec-tokens.mdx per arbiter fix list (fresh subagent)

Fresh Fixer. Never spawn subagents, never commit. Wartales pack only.
Read `docs/arbiter-tokens.mdx` in full, then execute ONLY its ordered
fix list (8 steps) against `design/spec-tokens.mdx`, verbatim, in
order, no reinterpretation. Change nothing else. Return: ≤8-line
summary — per-step ✓/✗.
