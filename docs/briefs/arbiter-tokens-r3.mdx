# Brief — Arbiter R3: design/spec-tokens.mdx review rulings (fresh subagent)

Fresh Arbiter, third attempt (two predecessors died mid-run — be
efficient: read ONLY what a ruling needs, never whole large files).
Never spawn subagents, never commit. Wartales pack only.

Input: `docs/review-spec-tokens.mdx` — 8 findings (F1-F8). For EACH:
read only the spec-tokens section it cites + spot-check the specific
evidence (a value in `design/extracted-ui/style.css` via grep, or one
number in foundation-guide §6). Rule ACCEPT/REJECT/MODIFY with
one-paragraph grounds. The spec text can win with receipts (e.g. if
style.css itself contains the "unsourced" accent value → REJECT F1
citing the line). Do NOT re-review untouched sections. Do NOT compute
more than 2 contrast ratios yourself (trust the reviewer's arithmetic
unless a ruling depends on it).

Deliverable: `docs/arbiter-tokens.mdx` — rulings table + verdict:
APPROVE-FOR-BUILD or REVISE with an exact ordered fix list. Return:
≤8-line summary with verdict. Keep total runtime under ~20 tool calls.
