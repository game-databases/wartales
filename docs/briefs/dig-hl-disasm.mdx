# Brief — Data dig 8: HashLink operand disassembler (fresh subagent)

Fresh data-layer dig agent. **Never spawn subagents** — you do all work
yourself. Never commit. Wartales pack only (`C:\_reps\game-databases\wartales`).
Headless session: stdout goes to a log; end with deliverable paths +
verification results.

Read first, in full: repo-root `AGENTS.md`;
`_foundation/extraction-doctrine.md`; `wartales/docs/decompile-dig-1.mdx`
(the plan §3 steps 1–5 are your roadmap); `wartales/docs/data-dig-log.mdx`
Dig 6 fork-bytecode facts + orientation-evidence.mdx §2b;
`wartales/extracted/logic/hl-structure/WALK-PROOF.mdx`;
`wartales/extracted/PROOF.md` R3 row; `wartales/docs/formulas-inventory.mdx`.

## State (verify, don't trust)

- Structure layer COMPLETE and verified at
  `extracted/logic/hl-structure/`: functions.jsonl (52,114),
  types.jsonl (46,891), globals.jsonl (34,258), constants.jsonl (27,941),
  natives.jsonl (569), strings.txt (75,321), opcodes.jsonl.gz
  (2,508,050 opcodes), debug resolution 52,114/52,114. Independent
  cross-check (crashlink) agreed element-wise incl. sampled full opcode
  sequences (`_crosscheck.py`).
- Pinned fork facts (measured, cite them, extend them):
  - Field ops carry flattened per-type field ordinals (root→leaf):
    `h2d.Camera.set_x/y/anchorX` → 0/1/6; `$Math.PI` resolves via
    `hl.BaseType→hl.Class→$Math` chain at ordinal 8.
  - Jump targets are signed offsets, destination = `pc + 1 + t`.
  - Call-family opcode 28 has a fixed 6-operand layout — left undecoded;
    decoding it is part of this dig.
- Goal of the broader effort (R3): operand disassembly → CFG → structured
  text mirrors under `extracted/decompiled/hl/{module}.hx` for the 1,609
  debug source paths. Formula recovery (damage/stat/price/XP/capture) is
  the product motive — calculators depend on it.

## Hard rules

- Python `C:\Python314\python.exe`, stdlib only.
- Never Read binary files into context; work through scripts.
- Never invent semantics. An opcode whose meaning is unproven stays
  `UNK` in output and in the ledger — guessed semantics are worse than
  none (they poison every downstream formula).
- Validate against known-semantics functions (the Dig 6 set:
  `WorldMesh.getFow`, `MiniMap.getPlayerPosition`, `ui.comp.Compass.sync`,
  …) until operand rendering reproduces their established behavior.

## Task

1. **`pipeline/tools/hl_disasm.py`**: render every function's opcode
   stream to operand-resolved text — fields by name path (flattened
   ordinals → root-type field chain), jumps by target label, globals/
   constants/strings/natives resolved to names, calls resolved to
   function names. Decode the call-family op 28 layout from evidence.
   Unknowns ledgered to `extracted/decompiled/hl/_unknown-opcodes.jsonl`.
2. **Emit complete disassembly** under `extracted/decompiled/hl/` mirroring
   debug source paths (`{module}.dis.hx` text or `.jsonl` — pick one,
   justify). 2.5M ops is minutes per Python pass — full coverage expected,
   no sampling shortcuts.
3. **CFG builder** (library function in hl_disasm.py): basic blocks +
   edges per function; cyclomatic stats per module to
   `output/_dig-disasm/cfg-stats.json`. Spot-verify 10 functions against
   hand-traced control flow.
4. **Formula probe**: disassemble-and-read the modules behind
   `formulas.jsonl` entries + combat damage / trade price / capture odds /
   XP candidates; write `docs/disasm-formula-findings.mdx` naming exact
   functions + op ranges that hold each formula, what is now readable,
   and what needs the structured-text step.
5. Structured-text decompiler: attempt ONLY if steps 1–4 land verified;
   otherwise record precisely how far the plan reached and what remains
   (decompile-dig-1 §3 step tracking).
6. Append **Dig 8** to `docs/data-dig-log.mdx`; update PROOF.md R3 row
   with measured facts only.

## Deliverables (exact paths)

`pipeline/tools/hl_disasm.py` · `extracted/decompiled/hl/**` (complete
disassembly + unknown-opcode ledger) · `output/_dig-disasm/` proofs ·
`docs/disasm-formula-findings.mdx` · Dig 8 log entry.

Do NOT touch `site/`, `run_all.*`; never commit; never spawn subagents.
