# Brief — Documentator R2: revise wartales/spec.md (fresh subagent)

Fresh Documentator, round 2. Never spawn subagents, never commit.
Wartales pack only. Your predecessor drafted `wartales/spec.md` with
`[PENDING-SCOUT]` holes; BOTH awaited inputs have since landed:

- `wartales/docs/competitor-research.mdx` (inventory, relationship
  model, pain points, scoring, applied relinking delta)
- `wartales/docs/toolchain-validation.mdx` (wtpak.py primary reader,
  all four paks exact; HashLink bytecode verdict; texture path;
  fallbacks A-D; scope mapping)

Read first: root AGENTS.md; FRAMEWORK.md §4 + §2; your predecessor's
spec.md in full; BOTH reports above in full; tools-plan.md (now
exists — fold its scored list into the tools block with citations).

Task: revise `wartales/spec.md` in place —
1. Close EVERY `[PENDING-SCOUT: competitor-research]` and
   `[PENDING-SCOUT: toolchain-validation]` hole with cited facts.
2. Entities: align kinds + source containers with the recon TSV
   evidence and the toolchain scope mapping (CDB/EXP/PRE/UIA/CHARS
   shorthands stay; upgrade readiness notes from "unproven" to the
   toolchain verdicts where proven).
3. Relations: adopt competitor-research §7's eight must-have joins +
   the eight extraction-only edges as the relations list seed.
4. Tools: replace seed list with tools-plan.md scored list (flagship
   = company builder).
5. Keep spec-frozen false. Keep every claim cited. Never invent.
Return: ≤12-line summary of what changed + any hole you could NOT
close (with why).
