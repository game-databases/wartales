# Brief — Documentator: spec amendments W5 (fresh subagent)

Fresh small Documentator agent. **Never spawn subagents**; never commit;
Wartales pack only. Headless: stdout → log; end with a change list.

Read first: `wartales/docs/arbiter-stages-build-r1.mdx` (item W5 is your
entire scope); `wartales/docs/spec-stages-datasets.mdx`; the review docs
it cites for context.

## Task

Apply arbiter item W5 to `docs/spec-stages-datasets.mdx` exactly:

1. R8 resolution: document the promote_drafts `--buildid` flag as a
   compliant §3.6-sanctioned addition; amend §3.4 with the ruled rule
   line.
2. F13: full matrix-envelope enumeration in §4.2 (replace the
   sketch-level description so tests can pin every field).

Nothing else — no rewrites beyond the two ruled amendments, no code,
no other files. Report each edit as section anchor + before/after gist.
