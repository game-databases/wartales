# Brief — Visual Critic round 2: F1 tokens (fresh subagent, HARSH)

You are the HARSH visual critic, round 2. Never spawn subagents, never
commit. Wartales pack only. Grade RENDERED OUTPUT, never summaries.

1. Render locally (local file only): `site/src/styles/tokens-preview.html`
   → `design/critique/preview-r2.png` (headless Edge/Chrome on this
   machine, --window-size=1600,1000).
2. Read your round-1 verdict (`docs/critique-tokens-r1.mdx`) and its
   re-entry bar: "with only the r2 preview on screen and no captions,
   the panel anatomy, type voice, and rarity slots must be
   attributable to Wartales before the palette is even considered."
3. Grade the NEW preview against `design/sources/` shots 02/03/20
   blind: cover the captions; would you believe panel anatomy, type
   voice, rarity slots belong to Wartales' product family? Then the
   §5 feel bar: finished-object feel, empty well honesty, gold
   discipline, hover vocabulary present.
4. Verdict: WOWED or SEND-BACK (with the single biggest gap + fix).
   Round law: no fixed number of rounds; be genuinely harsh, not
   performatively harsh — if it passes, say so plainly.

Write `docs/critique-tokens-r2.mdx`. Return ≤6 lines.
