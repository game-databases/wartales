# Wartales — F5 header search spec (piece F5)

Status: IN REVISION r2 for Arbiter → CodeWriter+TestWriter · authored
2026-08-26 by the Documentator from brief
[`docs/briefs/documentator-f5-search.mdx`](./briefs/documentator-f5-search.mdx);
revised r1 2026-08-26 — all twelve findings of
[`review-spec-f5-search.mdx`](./review-spec-f5-search.mdx) resolved, see
[Revision r1](#revision-r1); revised r2 2026-08-26 from brief
[`docs/briefs/specfixer-f5-r2.mdx`](./briefs/specfixer-f5-r2.mdx) —
arbiter residuals N-1/N-2/N-3 closed, erratum E-1 applied (route table
derived-from-F2-§6), see [Revision r2](#revision-r2); cross-spec
dependencies in [r1 conflicts](#r1-conflicts).
Inputs read in full: root `AGENTS.md`,
`_foundation/decision-register.md` (binding entry:
[DR-2026-08-22-search-is-not-a-page]),
`_foundation/localization-architecture.md`, `_foundation/site-sections.md`,
`_foundation/seo-standard.md`, `_foundation/design-standard.md`,
`_foundation/domain-doctrine.md`, `FRAMEWORK.md` §2/§4/§8,
[`../spec.md`](../spec.md) (frozen),
[`pieces.mdx`](./pieces.mdx) (F5 row + sequencing law),
[`locale-key-convention.mdx`](./locale-key-convention.mdx),
[`cdb-kind-map.mdx`](./cdb-kind-map.mdx),
[`../pipeline/tools/wave_kinds.py`](../pipeline/tools/wave_kinds.py),
[`../run_all.ps1`](../run_all.ps1) stage registry,
[`../PROGRESS.mdx`](../PROGRESS.mdx).

Shorthands: **DR** = the decision-register entry
[DR-2026-08-22-search-is-not-a-page] · **avail** =
`extracted/relinks/locale_availability.jsonl` · **overlay** =
`extracted/locales/<locale>/<kind>.json` · **wkk** = `wave_kinds.py` ·
**NWG** = new-world.guide (design-standard §2 structural reference).

Batch posture (PROGRESS execution plan): this is Batch-1 *spec prep* —
spec-writing is planning, not frontend, so it precedes the rule-8 gate;
**the build itself stays gated** on the Batch-2 completeness arbiter
(pieces sequencing law: F4/F5 land after the gate, before PT03/PT04).

## 0. Scope in one line

Ship the three halves of piece F5 — (a) a deterministic pipeline emitter
that turns the locale overlays + avail into a per-locale JSON search-index
artifact under `site/public/data/search/`, (b) ONE pure matcher module
`searchRows` importable identically by server and browser code, and (c) the
header search field that swaps results in place per the DR — plus the
negative contract that no `/search` route exists anywhere. Nothing else in
the shell changes.

## 1. Authority chain

The DR is binding, every clause. Quoted verbatim:

> 1. **There is no search route.** `/en/search` is a 404 — absent from the
>    header, the footer, `robots.txt` and the capture list.
> 2. **The header owns the field.** Closed, "Search" is a word in the nav.
>    Open, it is an input that grows across the row the nav was using.
> 3. **Results replace the page's own content in place** once two
>    characters are typed; the content returns when the field is cleared
>    or Escape is pressed. The content is hidden, never unmounted, so
>    scroll position and state survive the detour.
> 4. **Two characters** — one letter matches most of the catalog and the
>    reader has not said anything yet. The number is the owner's.
> 5. **One matching function, both sides.** `searchRows` runs on the
>    server and in the browser, so what matches cannot depend on which
>    side answered.

Clause 1's literal `/en/search` maps onto this pack's locale scheme as
follows: EN is the pivot at bare paths
([DR-2026-08-20-locale-urls]), so the route that must not exist is
`/search`; a `/{pivot}/*` spelling follows the pivot-prefix policy (301 to
bare path) and must therefore end in a 404, never a 200. Section 6 makes
both executable.

Other bindings consumed here:

- **pieces.mdx F5 row**: "Header search field (no /search route; 2-char
  trigger; in-place swap; same matcher both sides)", data dep = **search
  index artifact**, reference bar = the DR.
- **FRAMEWORK §2.9**: shipped JSON index; index is a *derived, disposable
  artifact*, not a data plane (§2.1); the static JSON API (§2.10) publishes
  the search index. §2.9 names MiniSearch under ~100k entities — departed
  from deliberately, reason recorded in
  [§4.6](#4-6-decision-record-minisearch-not-adopted-for-v1).
- **[DR-2026-08-22-inputs-answer-as-you-type]**: the field acts reactively
  on typed content; gated clause — no `<form>`, no `type="submit"` anywhere
  in the delivered tree.
- **[DR-2026-08-22-copy-earns-its-place] §3**: interface-explaining
  sentences banned — the named example is literally "a search empty-state
  teaching how to search".
- **AGENTS.md rule 8 / [DR-2026-08-19-data-before-frontend]**: frontend
  consumes contracts, never derives data — hence the index is emitted by
  the pipeline (where the data lives), and the browser only fetches and
  matches.
- **localization-architecture §1/§2/§4/§5**: one page per entity per
  locale; availability drives membership; chrome i18n is its own namespace;
  filler policy `omit-until-translated` (frozen in
  [`../spec.md`](../spec.md)).
- **seo-standard §2** "noindex internal search results" is superseded for
  this pack by the stronger DR form: there are no search-result URLs at
  all, so there is nothing to noindex — only absence to prove.
- **[DR-2026-08-18-pipeline]**: the emitter joins the single entrypoint
  (`run_all.ps1` stage `emit`), idempotent, standalone-runnable,
  EXTRACTION-LOG-registered.
- **design-standard §3/§5**: selected state honesty, no caption chrome,
  12px floor for the visible word, quiet confidence (numbers/counts, no
  narration).

## 2. Ground truth and vocabulary

- **Locales** (frozen in spec.md, client-verified): `en, fr, de, es, pl,
  pt-BR, ru, ko, zh` — exactly nine. Three vocabularies stay distinct
  everywhere in this piece (r1):
  - **client code** — the extraction/data-plane identity: overlay reads,
    artifact file names, artifact keys, `manifest.locales` (`pt-BR`,
    `zh` verbatim);
  - **URL segment** — F4's routing id (F4 §2 table, lowercase; recorded
    decision r1 there): `pt-br`, never `pt-BR`; pivot `en` carries no
    segment at all. Used ONLY to compose `href`
    ([§3.3](#33-the-href-route-table-declared-seam));
  - **BCP-47** — `<html lang>`/hreflang only (F4-owned; unused here).
  An emitted href never contains a client code where the segment
  differs. `ja` is community-lane, `omit-until-translated` — **never**
  in the artifact.
- **Kinds**: the canonical inventory is `MANAGED_KINDS` in wkk — 40 kinds,
  asserted so in the module. Kind-vs-sheet trap stands: kind `recipe`
  (sheet `craft`), kind `class` (sheet `unitClass`). Facet values in this
  feature are always kind names from that set, never sheet names.
- **Searchable subset**: a kind enters the index only when it has (a) a
  route contract and (b) localized names. Today that is exactly the wave-1
  bridged kinds:

  ```python
  SEARCHABLE_KINDS = ["item", "skill", "class"]   # frozen; ⊂ MANAGED_KINDS
  ```

  Growth procedure is the wkk precedent (`EXPECTED_PAIR_FILES` re-freeze):
  when R9/dig-14 lands wave-2 overlays, the list is extended in the
  emitter and the frozen numbers below are re-derived in the same
  commit. Until then the emitter refuses any kind whose overlays are
  absent (exit 2), so silent partial waves cannot ship.
  TypeScript never re-declares this list: the manifest publishes it
  (`kinds`) and the matcher surfaces it as `KIND_ORDER`
  ([§4.1](#41-module-rules)); the honesty chain is mechanical end to end
  ([§9](#9-test-spec-for-testwriter) suites 4–5).
- **Membership source**: avail, schema
  `{kind, id, availableLocales[], namedLocales[], fields:{<locale>:{name:bool,...}}}`
  (locale-key-convention §3). A row exists for locale `L` iff
  `L ∈ namedLocales`.
- **Name values**: read from overlay `<locale>/<kind>.json`, shape
  `{ "<entityId>": { "name"?: str, "description"?: str } }`
  (locale-key-convention §3). The value key is `description`, measured
  from disk at r2, not assumed: every entry of the 27 searchable overlay
  files ×9 carries `description` (29,889 entries; zero raw `desc`) — the
  bridge column name `desc` is aliased to `description` by the emitter
  and survives raw only in wave-2 non-searchable overlays
  (`tutorial.json`). Names may carry Heaps rich-text markup — the
  matcher strips it before normalizing ([§4.3](#4-3-name-normalization)).
- **Frozen numbers** (recounted from disk at r1, buildid 20318128;
  kinds recount refreshed at r2 on the post-regen generation;
  locale-key-convention §5/§7 agrees): the searchable universe is the
  `item`/`skill`/`class` subset of avail — avail itself carries 34 kinds
  (14,456 rows); the subset measured 2026-08-26:
  - universe **3,779** rows (item 2,125 / skill 1,373 / class 281);
  - **458** with no bridge leaf in *any* locale (item 318 / skill 137 /
    class 3) — `availableLocales` empty;
  - **102** with ≥1 resolved field but no name in any locale (item 4 /
    skill 98 / class 0) — a bridge leaf survives, a name never does, so
    they can never contribute a row under the membership rule above;
  - **3,219** with `namedLocales` non-empty (item 1,803 / skill 1,138 /
    class 278) — identical membership in all nine locales, zero
    partial-coverage rows; `fields[L].name` ↔ `namedLocales` flag parity:
    0 mismatches in both directions ×9.

  The arithmetic closes: 458 + 102 + 3,219 = 3,779. Consequences this
  spec pins — exact values, no bands:

  - a row exists for locale `L` iff `L ∈ namedLocales`, so each of the
    nine files carries exactly **3,219 rows**;
  - the class facet contributes exactly **278 rows per locale**;
  - the id-set is byte-identical across locales (perfectly parallel
    presence).

Recount behind every number above (pack root; executed at r1):

```python
import json, collections
rows = [json.loads(l) for l in
        open("extracted/relinks/locale_availability.jsonl", encoding="utf-8")]
sub = [r for r in rows if r["kind"] in ("item", "skill", "class")]
print(len(rows), len(set(r["kind"] for r in rows)))                            # 14456 34
print(len(sub), collections.Counter(r["kind"] for r in sub))                   # 3779 …2125/1373/281
print(sum(not r["availableLocales"] for r in sub))                             # 458
print(sum(bool(r["availableLocales"]) and not r["namedLocales"] for r in sub)) # 102
print(sum(bool(r["namedLocales"]) for r in sub))                               # 3219
```

These anchors freeze with buildid 20318128. A corpus regen re-runs the
recount and re-freezes this block plus the emitted manifest in the same
commit — the SEARCHABLE_KINDS growth law applied to numbers.

## 3. Contract — the search-index pipeline artifact

### 3.1 Emitter

New tool `pipeline/tools/search_index_emit.py`, registered in stage
`emit` as the **third step, after `validate_all.py`**
([`../run_all.ps1`](../run_all.ps1) `Invoke-StageEmit`), so stage 6 keeps
its existing steps byte-for-byte and the index is emitted only after the
canonical planes validated:

```
pipeline/tools/locale_bridge_dig.py
pipeline/tools/validate_all.py ...        (unchanged)
pipeline/tools/search_index_emit.py --buildid <BuildId>
```

CLI: `--availability`, `--overlays`, `--out`, `--buildid` all with
EXTRACTION-LOG-resolved defaults; `--list`-style help; standalone run
allowed ([DR-2026-08-18-pipeline]). Exit codes follow the house pattern:
`0` ok, `2` precondition/consistency failure (names the offending file),
`3` usage.

Inputs read: avail + `extracted/locales/<locale>/{item,skill,class}.json`.
Nothing else — the emitter derives no game data (rule 8); it selects,
shapes, and places rows.

Consistency gates (each a hard exit 2):

- every availability row whose kind ∈ SEARCHABLE_KINDS and
  `fields[L].name is true` must have a `name` string in overlay `L` —
  flag-without-value is corruption;
- every overlay name entry must correspond to an availability row with the
  flag true in that locale — value-without-flag likewise;
- locale set of overlays ∪ avail ∩ SEARCHABLE_KINDS must be exactly the
  nine official codes.

### 3.2 Output shape (under `site/`)

```
site/public/data/search/manifest.json
site/public/data/search/en.json          fr.json  de.json  es.json
site/public/data/search/pl.json          pt-BR.json
site/public/data/search/ru.json          ko.json  zh.json
```

Per-locale file — compact JSON with FIXED key order as illustrated (row
objects `kind,id,name,href`; top level `schema,locale,buildId,rows`),
`ensure_ascii=False`, UTF-8, LF — never `sort_keys`, whose
`href,id,kind,name` output would contradict the illustration and the
published schema ([§3.4](#34-determinism)) (r1):

```json
{
  "schema":  "wartales/search-index@1",
  "locale":  "de",
  "buildId": "20318128",
  "rows": [
    { "kind": "item", "id": "A1AstronomerLetter",
      "name": "Astronomers Letter", "href": "/item/A1AstronomerLetter" }
  ]
}
```

Row keys are exactly `{kind, id, name, href}` — four, no more. `name` is
the game string for that locale, markup included (stripping is a matcher
concern, [§4.3](#4-3-name-normalization)); the artifact stays the faithful
data plane and machine consumers get the raw name (§2.10 static JSON API).
**No normalized column is shipped**: normalization implemented twice
(Python emitter + TS matcher) is a drift engine, and the matcher computes
it lazily over ≤3.3k rows in negligible time — one implementation, in
TypeScript, is the whole guarantee.

`manifest.json`:

```json
{
  "schema":   "wartales/search-index@1",
  "buildId":  "20318128",
  "locales":  ["en","fr","de","es","pl","pt-BR","ru","ko","zh"],
  "kinds":    ["item","skill","class"],
  "rowCount": { "en": 3219, "fr": 3219, "de": 3219, "es": 3219,
                "pl": 3219, "pt-BR": 3219, "ru": 3219, "ko": 3219,
                "zh": 3219 }
}
```

`rowCount` values are the measured §2 anchor (3,219 named per locale),
not an illustration — the emitter computes them and suite 4 asserts
`rowCount[L] === rows.length === 3219`.

No timestamps, no observation times — determinism is the contract; the
buildId is the only freshness stamp (machine plane, D2-compatible).

### 3.3 The href route table (declared seam)

**derived-from-F2-§6** (r2). `href` is composed **at emit time** from one
declared table — links are data, not something the frontend derives.
The page shape is F2 §6's frozen URL law verbatim (re-checked against F2's
own concurrent r1 revision before freezing): entity pages WITHOUT a `/db`
prefix — pivot `/{kind}/{id}`, locale `/{P}/{kind}/{id}` over the
lowercase PAGE-plane presentation ids (`{P}` = `de es fr ko pl pt-br ru
zh`, paired with the DATA-plane client codes, `pt-br` ↔ `pt-BR`); `/db`
is reserved for PT02's landing/taxonomy surface and never appears in an
entity href
([spec-f2-contracts.mdx §6](./spec-f2-contracts.mdx#6-static-json-api-url-architecture)).
`{seg}` is the **URL segment**
taken from F4's routing table — the same presentation ids F2 §6 pins (F4
§2 id column — lowercase; client codes
never leak into hrefs, [§2](#2-ground-truth-and-vocabulary)):

| client code (artifact key) | URL segment | pivot href | non-pivot href |
|---|---|---|---|
| en | *(pivot — none)* | `/{kind}/{id}` e.g. `/item/A1AstronomerLetter` | — |
| de es fr ko pl ru zh | same as code | — | `/{seg}/{kind}/{id}` |
| pt-BR | `pt-br` | — | `/pt-br/{kind}/{id}` |

Concretely: a Portuguese hit emits `href="/pt-br/item/X"` — composed
from F4's segment (`pt-br`), which F4 routing serves (unknown segments
404, F4 §3 rule 5). Composing from the client code would ship
`/pt-BR/…` and dead-link every result in that index (r1 finding M1,
closed by construction). The pivot composes bare per
[DR-2026-08-20-locale-urls]. If F2 §6 ever renames the shape, its own
rename-and-301 law governs ([F2 §6](./spec-f2-contracts.mdx#6-static-json-api-url-architecture),
[F2 §4](./spec-f2-contracts.mdx#4-stable-identity-and-the-kind-universe))
— and the change here stays an emitter-table edit plus a rerun, never a
frontend change.

### 3.4 Determinism

Two consecutive runs produce byte-identical files (sha256 over all ten).
Rows sort by `(SEARCHABLE_KINDS index, id)`; objects serialize with fixed
key order — the §3.2 illustrated order; the published schema mirrors it,
so emitter and contract cannot disagree (r1); no clock, no randomness.
This is what makes the artifact
assertable and patch-diffable (E3 versioned truth rides the buildId).

## 4. Contract — the `searchRows` matcher

### 4.1 Module rules

- Location: `site/src/lib/search/searchRows.ts` +
  `site/src/lib/search/normalize.ts`. Pure TypeScript, no runtime deps.
- Imports of these modules may touch **nothing environmental**: no
  `window`, `document`, `navigator`, `localStorage`, `fetch`, no `node:`
  builtins, no React. Server code (future map search, API emitters) and
  the client island import the same file — DR clause 5 is satisfied by
  construction, and an executable guard enforces it
  ([§10](#10-acceptance-criteria) AC-11).

```ts
export interface SearchRow  { kind: string; id: string; name: string; href: string }
export interface SearchHit  { row: SearchRow; tier: 1 | 2 | 3 }
export const MIN_QUERY_CHARS = 2;   // DR clause 4 — the owner's number
export const KIND_ORDER = ["item", "skill", "class"] as const;
// tie-break + grouping order. NOT an independent literal: suite 4 asserts
// deep-equality with manifest.kinds each run, and the Python side asserts
// manifest.kinds == SEARCHABLE_KINDS ⊆ MANAGED_KINDS (suite 5).
export function searchRows(rows: readonly SearchRow[], query: string): SearchHit[];
```

`searchRows` returns ALL hits, best-first; rendering caps are the
component's business ([§5.4](#5-4-results-view)). It returns `[]` for
`normalized(query).length < MIN_QUERY_CHARS` — defense in depth; the
component never calls it below the threshold anyway.

### 4.2 Matching tiers

Let `q = normalizeName(query)` and `n = normalizeName(row.name)`:

- **Tier 1** — `n === q` (exact).
- **Tier 2** — prefix at a word edge: `n.startsWith(q)`, or `q` occurs at
  position `i > 0` with `n[i-1] === ' '` (post-normalization the only
  separator is the space).
- **Tier 3** — `n.includes(q)` (substring; this is the tier that makes
  zh matching work, since normalization leaves hanzi intact).

Total order over hits: `tier asc`, then `n.length asc` (shorter name =
more specific), then `row.kind` in `KIND_ORDER` order (= the emitter's
`SEARCHABLE_KINDS`, published as `manifest.kinds`), then
`row.id asc` lexicographic. Shuffling the input array cannot change the
output order — asserted by test.

### 4.3 Name normalization

`normalizeName(s: string): string`, defined once in `normalize.ts`:

```ts
s.replace(/<[^>]*>/g, " ")           // 1. Heaps rich-text tags (<b>, <img …/>) → space
 .replace(/\[[^\]]*\]/g, " ")        // 2. bracket macros ([NAME], [VALUE(…)], [ASSIGN]) → space
 .replace(/::[A-Za-z0-9_]+::/g, " ") // 3. ::color:: spans (::value::, ::count::) → space
 .normalize("NFKD")                  // 4. canonical + compatibility decompose
 .replace(/\p{M}/gu, "")             // 5. drop combining marks
 .toLowerCase()                      // 6. case-fold equivalent for our scripts
 .replace(/ł/g, "l")                 // 7. explicit fold — U+0142 has NO Unicode decomposition
 .replace(/\s+/g, " ").trim()        // 8. collapse whitespace
```

Why this exact pipeline covers the brief's zh/ko/ru/pl demands — every
claim below executed in Python `unicodedata` (same semantics as JS
`String.normalize`) at r1:

- **pl** — `ł` (U+0142) has neither a canonical nor a compatibility
  decomposition, so steps 4–5 leave it intact (`NFKD("Włóczęga")` +
  mark-strip yielded `włoczega` — the pre-r1 text asserted a `ł→l` fold
  the pipeline cannot perform); step 7 folds it explicitly:
  `Włóczęga → wloczega`, `Łódź → lodz`. Polish matching is not degraded.
- **ru** — NFKD splits `ё` (U+0451) into `е` + U+0308, so step 5 folds
  `ё → е`; accented/cased Cyrillic folds the same way. Query `Ёж`
  matches `еж`.
- **fr/de/es/pt-BR** — diacritics fold through NFKD + mark-strip
  (`Épée → epee`); case folds. `ß` does NOT fold (recorded gap,
  [H-5](#13-contract-holes-found)); Turkish `İ` incidentally folds to
  `i` and stays out of scope (no `tr` locale).
- **ko** — NFKD decomposes syllables to conjoining jamo (category Lo, so
  step 5 never eats them); compatibility jamo fold to choseong/jungseong
  forms only, never to jongseong, so `한국` and `ㅎㅏㄴㄱㅜㄱ` do NOT
  normalize to the same string (r2 correction of the r1 "normalize
  identically" claim). What holds — and all tier-2 matching needs — is
  the shared leading-jamo prefix: a partially-typed jamo query (`ㅎㅏ`)
  prefix-matches `한…` mid-word,
  `normalize("한국").startsWith(normalize("ㅎㅏ"))` (executed green in
  Node at r2, proof in [Revision r2](#revision-r2)) — desirable for a
  field that answers as you type
  ([DR-2026-08-22-inputs-answer-as-you-type]).
- **zh** — hanzi pass through NFKD/NFD unchanged (step 4 folds full-width
  variants, `ＸＹ → xy`); matching is plain substring, no segmentation needed at this
  corpus size.
- Steps 1–3 defend against game markup leaking into matching. Disk
  census over the searchable names ×9 locales (r1): 396 of 28,971 names
  carry `<tag>` markup (the oil-concentrate family,
  `<item>OilBrave</item> Concentrate`), 9 carry `[NAME]` macros
  (`AllyCorpse` — de `Leiche von [NAME]`), 0 carry `::color::` spans
  (descriptions carry 6,120; stripped anyway so the matcher stays
  reusable). Step 2 runs after step 1 so markup-wrapped macros die too;
  replacing with a space (not `""`) keeps words from fusing.

Known accepted gaps (recorded, not silently suffered): `ß` does not fold
to `ss`; macro stripping can leave harmless trailing punctuation
(`AllyCorpse` ru name → `тело ()`) — the residue trails the word, so
word-edge prefix matching is unaffected; Turkish `İ` out of scope
(no `tr` locale).

### 4.4 Both sides, one answer

The client island fetches `/data/search/<locale>.json` on first field-open
(lazy, cached in-module) and calls `searchRows`. Any server consumer
imports the same module and feeds it the same rows read from disk. There
is no second matcher, no server-side shortcut, no index-construction
config that could diverge — what matches cannot depend on which side
answered.

### 4.5 Complexity budget

Per keystroke: normalize ≤3.3k short strings + linear scan — low
single-digit milliseconds on commodity hardware, far inside the
scripted-interaction <200 ms CWV budget (FRAMEWORK §8). No debounce is
required at this size; if profiling ever says otherwise, debounce is a
component-local concern and must not touch the matcher.

### 4.6 Decision record: MiniSearch not adopted for v1

FRAMEWORK §2.9 names "shipped JSON index + MiniSearch under ~100k
entities". Departure reasons, for the arbiter to judge: (a) MiniSearch's
default tokenizer splits on non-alphanumerics and performs no CJK
segmentation — hanzi queries degrade to whole-field prefix and hangul
jamo typing breaks, i.e. it fails exactly the zh/ko bar the brief sets;
working around it means custom tokenizers, which reintroduces the
config-drift surface clause 5 forbids; (b) at ≤3.3k rows/locale a linear
scan is sub-millisecond, so the library buys nothing measurable; (c) the
shipped-JSON half of §2.9 is honored literally — the artifact IS the
index. Revisit triggers: any single-locale index exceeding ~100k rows, or
faceted *relational* filtering entering scope (§2.9's Meilisearch
threshold). Map-search reuse ([§12](#12-out-of-scope)) inherits
`searchRows` unchanged either way.

## 5. Contract — the header search field

### 5.1 Composition

- `site/src/components/header/SiteSearchField.tsx` — the field + state
  machine (`"use client"`).
- `site/src/components/header/SearchResultsView.tsx` — the in-place
  results surface.
- **Seam ownership (r1 — conforms to F4's landed r1 revision; both
  attributes are F4-owned):**
  - `data-shell-slot="search"` — F4's reserved mount in the header grid
    (nav row's right region), rendered empty until this piece lands.
    **F5 mounts into it**: the closed word control and the open input
    render inside that element; the attribute survives F5's takeover
    (F4's smoke greps for it). Integration is one composition edit:
    F4's stub `site/src/components/shell/search-slot.tsx` renders the
    field host instead of nothing ([§11](#11-files-touched)).
  - `data-search-swap-root` — **F4 server-renders it** on the shell's
    `<main>` content wrapper of every page (F4 §5 r1 inbound-seam
    bullet). F5 consumes it and touches nothing else of F4: no
    hidden-toggling from the shell's side, no search machinery in shell
    code.
- The swap target is the element carrying `data-search-swap-root`. On
  the 2-char crossing the field sets `hidden` on it and renders the
  results view into a container inserted immediately BEFORE that element
  (removed on restore) — results occupy the layout space the hidden
  content vacates. A missing swap root is a composition error (dev-mode
  invariant violation); there is NO header fallback render mode —
  surfacing results inside the header slot would contradict DR clause 3
  outright. Tests mount their own fixture roots carrying both
  attributes; the field never imports shell code.

### 5.2 States

```
closed ──activate(click/Enter on word)──▶ open
open   ──Escape───────────────────────▶ closed (query dropped, content restored, focus → word control)
open   ──clear(✕ control or emptying)─▶ open, content restored   (DR: "cleared … restores")
open   ──len(normalize(q)) ≥ 2────────▶ swapped (results replace content in place)
open   ──len drops below 2────────────▶ restored
```

Deliberate non-behavior: **blur does not close or restore** — the DR
names exactly two restore paths (cleared, Escape); inventing a third
(on-outside-click) would fight the in-place model. Recorded so the
reviewer does not "fix" it.

Recorded deviation (r1): the trigger thresholds key on
`len(normalize(q))`, not raw typed characters — the DR's "two characters
typed" is paraphrased onto the matcher's own alphabet. Consequence: some
two-keystroke inputs that normalize below two characters (whitespace,
combining pairs) never trigger, and one IME commitment landing ≥2
normalized characters does. Chosen because matching itself runs on
normalized text — a raw-length gate would arm the matcher with queries
it cannot meaningfully match. Recorded so the divergence from DR clauses
3/4 is deliberate, not unnoticed (same discipline as the blur note).

### 5.3 DR-verbatim behavior contract

Each clause with its enforcing mechanism:

1. *"Closed, 'Search' is a word in the nav."* — closed state renders the
   localized chrome word (`search.word`, en "Search") as a real control
   occupying only its word-width in the nav row. No icon-only ghost, no
   box.
2. *"Open, it is an input that grows across the row the nav was using."*
   — activation swaps the word for a text input laid out across the nav
   row. Owner: **F5**, delivered without editing any F4-owned shell file —
   the open state sets `data-search-open` on its mount element
   ([§5.1](#51-composition)) and F5's own stylesheet expands that mount
   across the nav row's columns while the nav items yield the row to it.
   Focus moves into the input. Visual styling rides F1 tokens; the
   structural claim (same row, full-row input) is asserted in the
   component suite against a fixture, and re-asserted against the
   composed shell at integration ([H-2](#13-contract-holes-found)
   family) (r1).
3. *"Results replace the page's own content in place once two characters
   are typed"* — once the normalized query crosses two characters
   (recorded deviation, [§5.2](#52-states)), `data-search-swap-root`
   (F4-placed, [§5.1](#51-composition)) gains the `hidden` attribute and
   the results view renders in the layout slot the content vacates
   (top-of-page framing: the reader sees results from the top, scroll
   reset is acceptable ONLY because the previous content is hidden — see
   next clause for what survives).
4. *"hidden, never unmounted, so scroll position and state survive the
   detour"* — the content subtree stays in the DOM (`document.contains`
   holds; React never unmounts it). Its scroll containers keep scrollTop;
   component state (open tabs, form entries) persists because no element
   is destroyed. Restoration removes `hidden` — the page continues
   exactly where it left.
5. *"the content returns when the field is cleared or Escape is pressed"*
   — both paths, plus the Escape collapse of [§5.2](#52-states).
6. Clause 1 (no route) is [§6](#6-contract--no-search-route); clause 5
   (one matcher) is [§4](#4-contract--the-searchrows-matcher).

### 5.4 Results view

- Grouped by kind in `KIND_ORDER` order (the manifest's `kinds`); each
  group header is the
  localized kind label + its hit count as a bare number (chip-word rules:
  a counted number carries no word beyond its label —
  [DR-2026-08-21-drop-chip-words-and-updated-column]).
- Every result row is a real `<a href>` to the artifact's `href` (crawl-
  able grammar even though the surface itself is transient), showing the
  game's own `name` for the locale. No descriptions, no icons in v1
  ([§12](#12-out-of-scope)).
- Render cap: first **50** hits overall (`MAX_RENDERED_ROWS = 50`, the
  network page-size choice [DR-2026-08-18-page-size]; a named constant,
  not a derivation). Group headers always show the FULL count from the
  complete hit set, so capping never lies about coverage.
- Zero matches: the count line reads `0` beside the echoed query — a
  plain-word fact. **No instructional copy, no suggestions module, no
  "did you mean"** ([DR-2026-08-22-copy-earns-its-place] §3; design-
  standard §5.4). Nothing teaches how to search.
- Faceting is the grouping itself; a kind-filter chip row is v1-optional
  and ships only if it stays purely client-side and reactive.

### 5.5 Input-gate compliance

No `<form>`, no `type="submit"` anywhere in the delivered tree
([DR-2026-08-22-inputs-answer-as-you-type] gated clause). The clear
affordance is `<button type="button">`. Activation of the closed word is
likewise `type="button"` with the word as its accessible name. Typing
drives everything; Enter navigates to the current top hit (link
semantics, not submission).

## 6. Contract — no search route

Negative space, enforced positively:

1. **Router**: no route file under any `app/**` path named `search` —
   ever, including nested and locale-segment variants. No redirect, no
   rewrite: a 301 would acknowledge the URL; the DR wants absence (404).
2. **Served behavior**: `GET /search` → 404; `GET /en/search` → 301 to
   `/search` (pivot-prefix policy) → 404 — net effect never 200.
   Executable once the app exists; until F4 lands, the checker below
   covers the static guarantees and the served check is listed as an
   integration criterion ([§13](#13-contract-holes-found)).
3. **robots.txt**: the string `search` appears in no directive (no
   `Disallow: /search` — that line would advertise the URL's existence).
4. **Sitemaps**: no `sitemap` source references `/search`; generated XML
   contains no such `<loc>`.
5. **Capture list**: the e2e crawl manifest (PT16-owned when it lands)
   never gains a `/search` entry.
6. **Internal links**: no `<Link>`/`href` in the delivered tree points at
   `/search`.

All six are swept by one executable test
(`search-route-absence.test.ts`, [§9](#9-test-spec-for-testwriter)) that
fails on any hit and reports the file+line — written so that it keeps
guarding the repo after F4/PT16 land (vacuous "tree absent" today is a
pass with an explicit `ABSENT:` report line, not a silent skip).

## 7. Keyboard and accessibility basics

- Landmark: the field sits in `role="search"`.
- The input carries `aria-label` (`search.fieldLabel`) — the visible word
  is gone once open, so the label must not depend on it.
- Live region: a visually-hidden `aria-live="polite"` span announces the
  hit count as a bare number after each keystroke-driven change. Plain
  words only; no enum vocabulary (user-language DR).
- `ArrowDown` from the input moves focus to the first rendered result
  link; `Tab` reaches results in DOM order (they follow the header).
  `ArrowUp` returns to the input.
- Escape: restore + collapse + focus the closed-word control (single
  stroke out, per [§5.2](#52-states)).
- Hit area, focus ring, and the visible word respect the 12px floor and
  token system (design-standard §3; F1 tokens).
- The results surface is preceded by an `h1`-level visually-hidden
  heading ("Search") so screen-reader users get a landmark boundary —
  chrome, not copy; it explains nothing.

## 8. Chrome i18n seam

Keys owned by this piece (namespace `search`), en values shipped,
consumed through a props/injected-dictionary seam with en fallback:

| key | en |
|---|---|
| `search.word` | `Search` |
| `search.clear` | `Clear` |
| `search.fieldLabel` | `Search` |
| `search.resultHeading` | `Search` |
| `search.countTemplate` | `{count}` |
| `search.kind.item` / `.skill` / `.class` | `Items` / `Skills` / `Classes` |

Non-locale work is deliberate: the chrome matrix belongs to F4
(pieces F4 row owns "chrome i18n namespace"); merging these keys into it
is the recorded handoff. Per localization-architecture §4 a locale's
chrome ships only when complete — until then non-pivot locales fall back
to the en strings rather than mixing prose. Placeholder is empty by
design (an instructional placeholder is banned copy). Game-kind facet
labels are chrome (site taxonomy), not game strings.

Merge mechanics (r1): when these keys enter `site/messages/<id>.json`
×9 they pass F4's gates mechanically — every `search.*` key lands in
`site/messages/_stub-keys.json` until translated (stubbed values stay
byte-identical across all nine files), `{count}` in `countTemplate`
participates in F4's `{placeholder}`-parity gate as a real placeholder,
and the merge commit DELETES this piece's interim props/injected-
dictionary resolver — next-intl becomes the only chrome mechanism;
two i18n mechanisms never ship together in a merged build
([H-4](#13-contract-holes-found)).

## 9. Test spec for TestWriter

Vitest (already the site runner, `site/package.json`). New
`site/vitest.config.ts` with two projects:

- **node** — matcher units, artifact assertions, route-absence sweep.
- **jsdom** (+ `@testing-library/react`, `jsdom`,
  `@vitejs/plugin-react` added to devDependencies) — the
  component suite. `react`/`react-dom` are NOT re-declared here: F4
  installs them as `dependencies` first (F4 §1), and re-adding them
  under devDependencies would create duplicate entries and version-skew
  risk (r1).

The three additive devDependencies are the only `package.json` edits
(`@types/react`/`@types/react-dom` already arrive with F4);
`npm test` stays `vitest run`.

Suites:

1. `normalize.test.ts` — table-driven, every case frozen to what the
   §4.3 pipeline actually yields (executed at r1): `ё→е` folding
   (`Ёж → еж`); fr accents (`Épée → epee`); pl explicit fold
   (`Włóczęga → wloczega`, `Łódź → lodz`); markup strip
   (`<b>X</b> → x`, `<item>OilBrave</item> Concentrate` →
   `oilbrave concentrate`); bracket-macro strip (`Leiche von [NAME]` →
   `leiche von`, `[Guard]X → x`); color-span strip
   (`<bad>::value::</bad> X → x`); ko jamo law (r2 — the r1
   `한국 ↔ ㅎㅏㄴㄱㅜㄱ equivalence` case was false under this pipeline
   and is deleted; frozen instead to what the §4.3 steps actually yield):
   `normalize("한국") === "한국"` (syllables decompose WITH
   jongseong finals), `normalize("ㅎㅏㄴㄱㅜㄱ") === "하ᄂ구ᄀ"`
   (compatibility jamo fold to choseong/jungseong only), the two NOT
   equal, and the prefix property
   `normalize("한국").startsWith(normalize("ㅎㅏ")) === true`;
   hanzi immutability
   (`长剑` stable); full-width fold (`ＸＹ→xy`); whitespace collapse;
   idempotence (`f(f(s)) === f(s)`).
2. `searchRows.test.ts` — **runs in BOTH projects** (same file, executed
   twice) asserting deep-equal outputs per query over a fixture corpus
   containing ru/fr/ko/zh names: tier assignment, total-order stability
   under input shuffle, `MIN_QUERY_CHARS` guard, empty-query `[]`.
   Identical arrays across projects is the executable form of DR clause 5.
3. `SiteSearchField.test.tsx` — renders with a fixture
   `data-search-swap-root` div pre-filled with scrollable content:
   - closed shows the word; activate → input focused, row-growth class
     present;
   - 1 char → root still visible; 2nd char → root `hidden === true` AND
     `document.contains(rootContent)` AND `root.scrollTop` preserved AND
     results groups render with counts and artifact hrefs;
   - clear control → restored, field still open; emptying text → same;
   - Escape → restored + collapsed + focus on the word control;
   - blur → neither closes nor restores (deliberate, [§5.2](#52-states));
   - rendered cap 50 with full group counts;
   - DOM sweep: zero `<form>`, zero `[type=submit]`;
   - zero-match shows `0` + echoed query and matches the banned-copy
     sweep (no "press/no results help/type" instructional strings).
4. `search-artifact.test.ts` (node) — reads
   `site/public/data/search/*.json` + manifest:
   - exactly the nine locale files, names == frozen official set;
   - manifest `rowCount[L] === rows.length` for all L; `buildId` present;
     `kinds` == `SEARCHABLE_KINDS`;
   - identical sorted `(kind,id)` list across all nine files;
   - every `kind ∈ SEARCHABLE_KINDS`; there is NO transcribed kind
     mirror — the TS constant `KIND_ORDER` is asserted deep-equal to
     `manifest.kinds` read from the artifact, closing mechanically what
     the Python side opens in (5);
   - row keys exactly `{kind,id,name,href}`; `name` non-empty; `href`
     starts `/`, contains no `/search`, matches the route table for its
     locale/kind — segment vocabulary enforced: pivot `en` bare,
     `pt-BR`-keyed file carries `/pt-br/…` hrefs (never `/pt-BR/…`),
     other locales `/{code}/…`;
   - class rows per locale == **278** (frozen anchor, §2); per-locale
     total == **3,219** exact (frozen anchor — no band, so even one row
     of membership drift fails; re-derived only via §2's recount-and-
     refreeze law) and equal across locales;
   - membership spot-bijection: sample rows verified against avail
     (`L ∈ namedLocales`).
5. `pipeline/tests/test_search_index.py` — synthetic mini avail + mini
   overlays in tmp: happy path byte-determinism (two runs, sha256 equal);
   flag-without-value → exit 2; value-without-flag → exit 2; unknown
   locale → exit 2; kind outside `SEARCHABLE_KINDS` refused (exit 2) —
   and this suite imports `wave_kinds.MANAGED_KINDS` directly and
   asserts the emitter's `SEARCHABLE_KINDS ⊆ MANAGED_KINDS` plus
   `manifest.kinds == SEARCHABLE_KINDS`: the honesty chain
   wkk → emitter list → manifest.kinds → TS `KIND_ORDER` is mechanical
   at every hop, no transcription anywhere (r1).

## 10. Acceptance criteria

Executable unless phrased otherwise; every command runs from the pack
root.

- **AC-1** (r1) — `pwsh ./run_all.ps1 emit` completes exit 0 and produces
  `site/public/data/search/{en,fr,de,es,pl,pt-BR,ru,ko,zh}.json` plus
  `manifest.json`; the pre-existing two emit steps are unchanged. Diff
  envelope, stated per scope so it is falsifiable: inside `run_all.ps1`
  the diff is EXACTLY the appended third step line plus the StageInfo
  `'emit'` description; across the whole tree the commit touches exactly
  `run_all.ps1`, `EXTRACTION-LOG.md` (registry entry),
  `pipeline/tools/search_index_emit.py`, and the ten emitted JSON files
  — nothing else (`site/package-lock.json` untouched by this commit).
- **AC-2** — a second immediate `emit` run leaves all ten files
  byte-identical (recorded sha256 pair in the run log).
- **AC-3** — bijection holds: for every locale L and every avail row with
  kind ∈ SEARCHABLE_KINDS, `L ∈ namedLocales ⇔` exactly one row in
  `L.json`; and every row of `L.json` satisfies `L ∈ namedLocales`.
  (Full scan, not sampled.)
- **AC-4** (r1) — the nine files carry one identical sorted `(kind,id)` list;
  class contributes exactly 278 rows per locale; per-locale total equals
  **3,219** exactly (frozen anchor §2 — the band is dropped: even one row
  of silent membership drift fails; totals re-derive only through §2's
  recount-and-refreeze law).
- **AC-5** (r1) — row/manifest shape exactness: row keys `{kind,id,name,href}`;
  `name` non-empty; `href` per the §3.3 table for its locale with the
  segment vocabulary enforced (`pt-br` lowercase from F4's segment law,
  pivot bare, never a client code where the segment differs); no `href`
  contains `/search`; manifest fields per §3.2; `vitest run` (node
  project) green including `search-artifact.test.ts`.
- **AC-6** — `pwsh ./run_all.ps1 --dry-run emit` prints the emitter as
  the third step, after `validate_all.py`, with resolved defaults;
  `--list` shows the emit stage description updated.
- **AC-7** — `pytest pipeline/tests/test_search_index.py` green:
  determinism, the three exit-2 corruption gates, and the
  non-searchable-kind refusal.
- **AC-8** — matcher parity: `vitest run` executes `searchRows.test.ts`
  under BOTH projects; all fixture queries (incl. `ё`→`е`, fr accents,
  ko jamo partial, zh substring) produce deep-equal arrays in both.
- **AC-9** (r2) — normalize unit table green: all §9.1 cases including
  the ko jamo law as re-frozen there (decomposed-syllable form,
  compatibility-jamo mapping, and the prefix property — NOT string
  equivalence) + idempotence.
- **AC-10** — component suite green: every §9.3 bullet, including the
  hidden-not-unmounted + scroll-preservation triple assertion and the
  blur non-behavior.
- **AC-11** — environment-purity guard: a test greps
  `normalize.ts`/`searchRows.ts` for `window|document|navigator|
  localStorage|fetch|node:` and fails on any hit.
- **AC-12** — input gate: rendered-DOM sweep proves no `<form>` and no
  `type="submit"` in the component tree.
- **AC-13** — route absence: `search-route-absence.test.ts` green — no
  `app/**/*search*` route file; `robots.txt` (if present) contains no
  `search` directive; no sitemap source or generated XML references
  `/search`; no internal `href` targets `/search`; each absent tree is
  reported as an explicit `ABSENT:` line.
- **AC-14** — a11y suite bullets green: `role="search"` present; input
  has `aria-label`; live region announces the count; ArrowDown/ArrowUp/
  Tab/Escape focus behavior per §7.
- **AC-15** — anti-slop sweep on rendered strings: none of the
  component's user-visible strings matches the instructional/absence
  patterns (how-to-search, "no results found"-style apologies); zero
  state renders count `0` + query echo only.
- **AC-16** — chrome seam: all §8 keys resolve for en; injecting a
  partial dictionary falls back to en per key without crashing (unit
  test on the resolver).

## 11. Files touched

New:

- `pipeline/tools/search_index_emit.py`
- `pipeline/tests/test_search_index.py`
- `contracts/search-index.schema.json` (published contract, F2 convention:
  row + manifest schemas, `SEARCHABLE_KINDS`, route table)
- `site/src/lib/search/normalize.ts`
- `site/src/lib/search/searchRows.ts`
- `site/src/lib/i18n/search-chrome.ts` (keys + en dictionary + fallback
  resolver)
- `site/src/components/header/SiteSearchField.tsx`
- `site/src/components/header/SearchResultsView.tsx`
- `site/vitest.config.ts`
- `site/src/lib/search/__tests__/normalize.test.ts`
- `site/src/lib/search/__tests__/searchRows.test.ts`
- `site/src/lib/search/__tests__/search-artifact.test.ts`
- `site/src/lib/search/__tests__/search-route-absence.test.ts`
- `site/src/components/header/__tests__/SiteSearchField.test.tsx`

Edited:

- `run_all.ps1` — **pack root** (r1: the stage registry lives at the
  pack root — `pipeline/run_all.ps1` does not exist, and §1/§3.1/AC-6
  already cite `../run_all.ps1` / `./run_all.ps1`)
  (emit stage: append step 3 + StageInfo desc)
- `EXTRACTION-LOG.md` (stage registry entry for the emitter)
- `site/package.json` (devDependencies, ADDITIVE ONLY:
  `@testing-library/react`, `jsdom`, `@vitejs/plugin-react` —
  react/react-dom arrive as F4 `dependencies`, never re-declared (r1))
- `site/src/components/shell/search-slot.tsx` (r1: F4-created stub whose
  null body is replaced by the field host — the one composition edit of
  the [§5.1](#51-composition) mount takeover)

Untouched by design: all existing pipeline tools, `extracted/**`,
tokens/styles, `run_all.sh` (pure forwarder).

## 12. Out of scope

- Icons/thumbnails on result rows (needs the icon CDN piece; rows stay
  text+facet).
- Description-text search, fuzzy scoring beyond the three tiers, pinyin/
  romaja transliteration, synonym rings.
- Meilisearch or any external search service (§4.6 revisit triggers).
- The map-page search reuse — the matcher is written to be reusable, but
  no map code ships here.
- Locale chrome translations beyond en (F4's matrix), URL-prefix
  machinery, the shell itself (beyond the one `search-slot.tsx`
  composition edit of [§5.1](#51-composition)).
- The served-404 check (`GET /search`) — executable only once an app
  exists; tracked as integration criterion H-2 below.
- Any UGC/analytics on the field (no query logging anywhere in this
  piece — nothing to log it with, by design).

## 13. Contract holes found

- **H-1 — RESOLVED (r2): route table derived-from-F2-§6.** The r1 text
  called §3.3's `/db/{kind}/{id}` provisional pending a PT02/PT03 IA
  choice — a misattribution: F2 §6 is already frozen and pins entity
  pages WITHOUT the `/db` prefix (`/{kind}/{id}`,
  `/{P}/{kind}/{id}` over the page-plane presentation ids;
  `/db` reserved for PT02's landing/taxonomy surface; FRAMEWORK's
  `/db/…` example demoted to evidence shape), with ids verbatim and
  slugs only ever additive
  ([spec-f2-contracts.mdx §6](./spec-f2-contracts.mdx#6-static-json-api-url-architecture),
  [§4](./spec-f2-contracts.mdx#4-stable-identity-and-the-kind-universe)).
  §3.3 now derives verbatim from that law; no IA choice is owed before
  DB-core pages land. Remaining law unchanged: if F2 §6 itself ever
  renames the shape, its rename-and-301 governs — emitter table edit +
  rerun; consumers of the artifact are unaffected.
- **H-2 — served-404 + capture-list legs need the app.** The DR's
  clause-1 guarantees are fully specified here; two of their proofs
  (served `GET /search` → 404; capture-list exclusion) become executable
  only when F4's router and PT16's crawl manifest exist. Both checks are
  pre-written in `search-route-absence.test.ts`'s sweep list — they
  activate as their trees appear.
- **H-3 — seam RESOLVED in both specs (r1); one residual edge.**
  Placement is settled: F4 §5 (r1) owns BOTH attributes — the reserved
  `data-shell-slot="search"` mount (nav row's right region, empty until
  F5) and `data-search-swap-root`, server-rendered on the shell's
  `<main>` of every page (F4 AC-11 asserts both). F5 consumes them
  ([§5.1](#51-composition)); the old header-fallback idea is deleted — a
  missing swap root is a composition error, never a degraded render.
  Residual edge recorded in [r1 conflicts](#r1-conflicts): F4's smoke
  emptiness leg is pre-F5-only.
- **H-4 — chrome key merge, mechanics pinned (r1).** §8's namespace
  merges into F4's `site/messages/<id>.json` ×9; until the merge the
  field self-hosts the keys via the props/injected-dictionary resolver.
  Merge-time obligations (F4's gates apply mechanically): all `search.*`
  keys enter `_stub-keys.json` until translated, byte-identical ×9 while
  stubbed; `{count}` participates in placeholder-parity; the interim
  resolver is deleted in the merge commit — next-intl becomes the only
  chrome mechanism. Key names remain the negotiation surface.
- **H-5 — `ß`/`ss` and future orthographic folds.** Recorded gap in
  §4.3; if German players report misses, the fix extends `normalizeName`
  (one place, both sides inherit it) — never a per-surface workaround.
- **H-6 — wave-2 growth.** `SEARCHABLE_KINDS` re-freeze (§2) must land in
  the same commit as R9's wave-2 overlays for those kinds — their
  kind-first page routes are already pinned by F2 §6, so no separate
  route freeze exists to pair with (r2); the emitter's refusal gate
  (exit 2) is what forces the reconciliation rather than trusting it.

## r1 conflicts

Cross-spec edges this revision depends on or leaves open. F4's r1
revision landed CONCURRENTLY with this one: its search-seam amendment
text (F4 §5 r1 bullets, F4 AC-11, F4 §7.6 smoke) was present on disk and
is conformed to above — F4 itself was never edited from here.

1. **SATISFIED — swap root.** F4 server-renders `data-search-swap-root`
   on the shell's `<main>` of every page and owns the reserved
   `data-shell-slot="search"` mount. Nothing further is owed to F5;
   ownership table lives in [§5.1](#51-composition).
2. **OPEN — F4's empty-slot assertion is pre-F5-only.** F4 §7.6 smoke
   and F4 AC-11 assert `data-shell-slot="search"` present AND EMPTY.
   True at F4; false the day F5 mounts the field into it.
   Reconciliation is owed on the F4 side (next F4 revision, or F5's
   integration commit if they land in one batch): once F5 fills the
   mount, that leg becomes "contains exactly the search-field control".
   F5 cannot fix it — `site/scripts/smoke.mjs` is F4-created and
   F4-owned.
3. **Segment law dependency.** §3.3 composes hrefs from F4 §2's id
   column (lowercase segments; client codes stay data-plane-only). If
   the orchestrator overrides F4's recorded `pt-br` casing decision,
   exactly one §3.3 route-table row flips — emitter edit + rerun, per
   H-1's law.

## Revision r1

Finding-by-finding ledger for
[`review-spec-f5-search.mdx`](./review-spec-f5-search.mdx)
(2026-08-26; 2 BLOCKER / 3 MAJOR / 7 MINOR — all twelve resolved with
disk evidence, none argued away).

| id | severity | resolution | sections touched |
|---|---|---|---|
| B1 | BLOCKER | Ceiling re-derived from my own avail recount (458 no-leaf + 102 nameless-anywhere + 3,219 named = 3,779); per-locale total pinned exact 3,219; invented `"en": 3247` manifest example replaced with measured nine × 3,219; AC band dropped for exact equality | §2, §3.2, §9 suite 4, AC-4 |
| B2 | BLOCKER | Seam conformed to landed F4 r1: both attributes F4-owned; field mounts into `data-shell-slot="search"`, consumes F4-placed `data-search-swap-root`; header-fallback deleted (missing root = composition error); grow-across-the-row owned by F5 via runtime attribute + F5 stylesheet | §5.1, §5.3.2, §5.3.3, §11, H-3, r1 conflicts |
| M1 | MAJOR | Href segment vocabulary pinned: compose from F4's lowercase ids (`pt-br`), pivot bare, client codes data-plane-only; route table gained the code→segment column | §2, §3.3, §9 suite 4, AC-5 |
| M2 | MAJOR | Files-touched path corrected to pack-root `run_all.ps1` (`pipeline/run_all.ps1` does not exist) | §11 |
| M3 | MAJOR | Pipeline executed over the frozen pl cases: U+0142 has no decomposition, so explicit step-7 `ł→l` fold added (`Włóczęga → wloczega` verified); suite-1 cases re-frozen to true outputs; matching-quality bar kept | §4.3, §9 suite 1 |
| m1 | MINOR | Strip extended: bracket macros + `::color::` spans (census: 396 tag-bearing names, 9 `[NAME]` names ×9 locales, 0 color-span names); trailing-residue gap ledgered | §4.3, §9 suite 1 |
| m2 | MINOR | Normalized-length trigger recorded as a deliberate DR-clause-3/4 deviation | §5.2, §5.3.3 |
| m3 | MINOR | react/react-dom not re-declared (F4 installs them as dependencies); additive devDeps named exactly | §9 preamble, §11 |
| m4 | MINOR | Transcribed 40-kind mirror deleted; mechanical honesty chain wkk → emitter → manifest.kinds → KIND_ORDER asserted at every hop | §2, §4.1, §4.2, §5.4, §9 suites 4–5 |
| m5 | MINOR | Chrome merge mechanics pinned: `_stub-keys.json` listing, `{count}` placeholder parity, byte-identical stubbed values ×9, interim resolver deleted at merge | §8, H-4 |
| m6 | MINOR | AC-1 diff envelope stated per scope: per-file inside `run_all.ps1`, exact file set across the tree | AC-1 |
| m7 | MINOR | Serialization pinned to the illustrated fixed key order; `sort_keys` explicitly excluded; schema mirrors it | §3.2, §3.4 |

AC count after revision: **16** — numbering unchanged; AC-1, AC-4, AC-5
bodies revised in place and tagged `(r1)`; every AC remains executable
by its named suite/artifact.

## Revision r2

Ledger for the [`arbiter-f5-search.mdx`](./arbiter-f5-search.mdx)
residuals (2026-08-26, brief
[`docs/briefs/specfixer-f5-r2.mdx`](./briefs/specfixer-f5-r2.mdx): one
MAJOR + two MINOR + routed erratum E-1). Corpus-churn disclosure: the
concurrent dig session rewrote overlays + avail at 07:14, AFTER the
arbiter's verifications (07:08) — so every claim below was re-measured
this pass against the settled generation (buildId 20318128) rather than
carried, and one arbiter premise inverted on the newer disk (N-2).

| id | resolution | sections touched |
|---|---|---|
| N-1 | MAJOR closed. The re-frozen r1 ko case `한국 ↔ ㅎㅏㄴㄱㅜㄱ equivalence` confirmed FALSE in Node under the exact §4.3 steps 1–8 (proof below) and is DELETED. Re-frozen to the three assertions that execute true: decomposed-syllable form, compatibility-jamo mapping, and the tier-2 prefix law `normalize("한국").startsWith(normalize("ㅎㅏ"))`; literals written codepoint-exact into §9 suite 1 and verified byte-on-disk. §4.3's ko bullet narrowed from "both sides normalize identically" to the shared-leading-jamo prefix property. AC-9 amended in place; AC-8 untouched (its "ko jamo partial" leg rides the prefix law). | §4.3, §9 suite 1, AC-9 |
| N-2 | MINOR closed by measurement — premise inverted. The r2 disk sample disproves the arbiter's claim: all 27 searchable overlay files ×9 (the emitter's only inputs) carry value key `description` — 29,889 entries, ZERO raw `desc`. The citation therefore does NOT flip to `desc`; instead §2 hardens with the measured evidence and names the alias: bridge column `desc` → emitted `description`, raw `desc` surviving only in wave-2 non-searchable overlays (`tutorial.json`). Freezing `desc` would have made the spec false on the current generation. | §2 (Name values) |
| N-3 | MINOR closed. Kinds recounted from disk on the settled generation: avail carries **34** kinds (14,456 rows), not 33 — frozen. Recount snippet extended to print `len(rows)` and the kind count so the number re-derives with every refreeze. Searchable-subset anchors reproduced identically post-regen (3,779 = 458 + 102 + 3,219; item 2,125 / skill 1,373 / class 281) — no other §2 number moved. | §2 (Frozen numbers + recount snippet) |
| E-1 | ERRATUM applied per arbiter route option (i). §3.3 re-derived verbatim from FROZEN F2 §6: entity pages WITHOUT `/db` — pivot `/{kind}/{id}`, locale `/{seg}/{kind}/{id}`; `/db` reserved for PT02's landing/taxonomy surface; table marked **derived-from-F2-§6**; §3.2 example href re-derived to `/item/A1AstronomerLetter`; H-1 rewritten RESOLVED naming F2 §6 (ids verbatim, slugs only additive, rename-and-301 law inherited); remaining PT02 route attributions deleted from §2's growth procedure and H-6. No IA choice owed before DB-core pages land. F2 §6 was RE-READ IN FULL before freezing because its own concurrent r1 revision landed mid-pass — the no-`/db` law is unchanged and its new page-plane presentation-id pairing (`pt-br` ↔ `pt-BR`) is adopted into §3.3's wording. | §3.2, §3.3, §13 H-1/H-6, §2 |

### N-1 execution proof (Node, §4.3 steps 1–8 verbatim, run twice this session)

```text
normalize(han)          = U+1112 U+1161 U+11AB U+1100 U+116E U+11A8
normalize(jamo full)    = U+1112 U+1161 U+1102 U+1100 U+116E U+1100
normalize(jamo partial) = U+1112 U+1161
equivalence normalize(han) === normalize(jamo full)        : false
prefix      normalize(han).startsWith(normalize(jamo partial)) : true
pin         normalize(jamo full) === conjoining U+1112 U+1161 U+1102 U+1100 U+116E U+1100 : true
```

(`han` = 한국 U+D55C U+AD6D; `jamo full` = ㅎㅏㄴㄱㅜㄱ U+314E U+314F
U+3134 U+3131 U+315C U+3131; `jamo partial` = ㅎㅏ.)

AC count after revision: **16** — numbering unchanged; AC-9 amended in
place and tagged `(r2)`; every AC remains executable by its named
suite/artifact. E-2 stays routed to the orchestrator (F4 AC-11 + §7.6
smoke legs — F4-owned files, untouchable from this piece).

## Errata candidates

Doc-only appendix (fix round r2, 2026-08-26, brief
[`docs/briefs/specfixer-f5-build-r2.mdx`](./briefs/specfixer-f5-build-r2.mdx)).
The two spec-letter divergences found by the F5 build review were
adjudicated **ACCEPT-AS-XFAIL** by both reviewers and are encoded as two
named strict-xfail markers in
[`pipeline/tests/test_search_index.py`](../pipeline/tests/test_search_index.py)
(fix-round-r2 split of test-review M-2). They are recorded here as
candidates for a FUTURE revision only. **Nothing in this section changes
any frozen law or AC above**: the landed emitter behavior, the suites,
and the xfail encoding are exactly as reviewed; adoption belongs to the
next spec revision, which re-executes §2's recount-and-refreeze
discipline for whatever numbers move.

### XF-A — §9 item 5's "kind outside SEARCHABLE_KINDS refused (exit 2)"

Measured behavior (probed independently by BOTH reviewers, 2026-08-26):
a non-searchable availability row is silently FILTERED and emit exits 0;
artifact purity holds positively
(`test_non_searchable_kind_never_reaches_the_artifact`, green).

- Code reviewer's ruling ([review-f5-code §D](./review-f5-code.mdx#d-the-two-strict-xfail-divergences)):
  "The letter cannot bind. §3.1's gate list … is exhaustive and contains
  no such gate; §2's own growth law expects avail to grow PAST the trio …,
  so a blanket exit-2 on any non-searchable row would brick stage `emit`
  on unrelated corpus growth." Errata candidate verbatim: "reword §9 item
  5's refusal clause to artifact-purity form ('a kind outside
  SEARCHABLE_KINDS never reaches the artifact'), and consider the gate
  actually worth having: exit 2 when a NON-searchable kind carries
  non-empty `namedLocales` — today a wave-2-ready kind ships silently
  unsearchable, which is the true partial-wave risk §2 names."
- Test reviewer's ruling ([review-f5-tests axis 4](./review-f5-tests.mdx#axis-4--the-strict-xfail-encoding)):
  strict-xfail "keeps executing, keeps the spec letter as the recorded
  expectation, tolerates today's measured reality, and FAILS THE SUITE
  the day the letter is adopted"; granularity residual M-2 (two
  divergences, one marker) closed in fix round r2.

**Suggested narrower gate for the next revision:** replace §9 item 5's
refusal letter with the artifact-purity form plus ONE targeted gate —
`exit 2` when an availability row whose kind ∉ SEARCHABLE_KINDS carries
non-empty `namedLocales`, naming the offending `(kind,id)`. That catches
the wave-2-ready-but-unsearchable case without bricking emit on inert
kinds (34 on disk today).

### XF-B — §3.1's locale-set union read as a directory sweep

Measured behavior: a stray overlay-only locale directory (`ja/`) is
ignored when avail carries exactly the official nine — the consistency
gate iterates the LOADED overlays dict (nine keys), not the directory
listing; and `extracted/locales/` carries non-locale directories TODAY
(`ui/`, `_draft-achievements/`), so a literal directory sweep would exit
2 on the production corpus.

- Code reviewer's ruling ([review-f5-code §D](./review-f5-code.mdx#d-the-two-strict-xfail-divergences)):
  "Measured disk reality kills the naive enforcement … The emitter reads
  overlays only through the nine-code lens …, so a stray dir contributes
  zero bytes to any artifact; and the plane that actually drives
  membership IS guarded — `ja` appearing in avail's fields/namedLocales
  trips exit 2 (`test_unknown_locale_in_avail_refused_exit_2`, green)."
  Errata candidate verbatim: "restate §3.1's locale-set gate over avail's
  locale vocabulary plus the nine READ paths, with an explicit allowlist
  for tool dirs; optionally warn on unknown *locale-shaped* dirs (`xx`,
  `xx-YY`) so a future official-locale addition cannot pass silently."
- Test reviewer's ruling: same axis-4 passage as XF-A; the probe evidence
  ("a stray `ja/` overlay directory is invisible because the locale-set
  gate iterates the loaded dict (nine keys), not the directory listing")
  was reproduced by subprocess at review time.

**Suggested narrower gate for the next revision:** keep the gate keyed to
avail's locale vocabulary ∪ the nine READ overlay paths (= today's
behavior, already exit-2-guarded); add an allowlist for known tool dirs
(`ui`, `_draft-*`), and downgrade unknown but LOCALE-SHAPED directories
(`/^[a-z]{2}(-[A-Z]{2})?$/`) to a printed warning so a future official
locale cannot be added as overlays-only and pass in total silence.

Also queued for the same next revision (from this round's code-review
minors, no frozen text touched): m-1 — AC-1/§11's diff envelope amended to
"artifacts local-only (gitignored per AGENTS.md rule 5); envelope = code +
registry + gitignore line", since the ten emitted JSONs can never satisfy
today's "ten emitted JSON files — nothing else" letter in a commit; and
m-2 — §11's stale premise that `site/package.json` devDependencies arrive
with F5 (they landed with F4's 6bd451d).
