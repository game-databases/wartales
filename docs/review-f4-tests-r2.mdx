# Delta review — F4 test-suite fix round r1

**Verdict: DELTA-CLEAN.** Both MAJORs are genuinely fixed (M-1 re-proven
red-first on my own rig, M-2's evidence law measured against a real Next 16
artifact, not assumed), all nine minors are closed as ledgered, N1/N3 are
minimal-correct, and the F4-era suites stand at **62/62 green including the
unmodified tokens canon** on the tree as it exists right now. The residuals
below are scope/attribution notes about the *surroundings* of the fix round
(a swept-in package.json hunk, concurrent F5 work that landed mid-review),
not defects in it; none reopens any finding of
[`review-f4-tests.mdx`](./review-f4-tests.mdx).

Reviewed delta: commit `6bd451d` (= HEAD) versus the reviewed state
`c4f312f`, judged strictly against r1's findings table and its
`## Fix round r1` ledger. Read in full: both prior review docs, the full
current text of every changed file (`shell-hygiene.test.ts`,
`smoke.mjs`, `locale-routing.test.ts`, `chrome-parity.test.ts` at its new
home, `locale-combobox.tsx`, `globals.css`), plus `locales.ts`,
`tokens.css`, `request.ts`, `tsconfig.json`.

## What was executed on this machine (not just read)

- **M-1 attack, my own rig** at `site/node_modules/.f4delta-r2/` (verbatim
  copies of the suite + `tokens.css` + `globals.css`; fixtures swapped
  between runs; config inside the rig so neither zero-config discovery nor
  F5's project config could see it; **deleted afterwards**, residue-checked):
  - CONTROL (`bg-bg-1 text-selection p-g4 m-g1 gap-g2 border-border`) →
    **11/11 green** — the new law raises no false positive.
  - PLASTER A (r1's exact string: `bg-gray-900 text-zinc-100
    border-neutral-700 hover:bg-slate-800 dark:bg-gray-800 from-stone-200/50
    ring-zinc-500!`) → **stock-palette law RED with exactly 7 violations,
    all 10 sibling laws green** — the precise inverse of r1's mutation A
    (11/11 GREEN against this same fixture under the old law).
  - PLASTER B (same + `p-4 gap-2 rounded-lg`) → **3 failed / 8 passed**:
    stock-palette AND numeric-spacing AND rounded-corner all red together —
    selectivity preserved.
  - PLASTER C (bare `gray-500` alone) → **stock-palette red, 1 violation** —
    the ban on bare stock classes survives the new `(?:^|-)` anchor.
  - Function-replica battery over the verbatim tokenizer: 12/12 stock
    spellings hit (incl. `md:text-neutral-300`,
    `focus-visible:ring-zinc-500/60!`, `max-lg:bg-stone-400`,
    `[color:gray-500]`, `bg-gray-50/950` ladder edges) and 9/9 legal tokens
    pass (`parchment-100`, `bg-[var(--bg-1)]`, `w-1/2`, …).
- **M-2 guard honesty**: ran `node scripts/smoke.mjs` disarmed myself →
  `smoke verdict: PASS — 1 passed, 0 failed, 19 skipped`, exit 0; the 19th
  skip is `build-prerender` naming `WARTALES_SMOKE_BUILD=1`/`--build`;
  `stage-check-pair` executed for real.
- **M-2 assertion, executed independently**: replicated the leg's exact
  assertion half (smoke.mjs:852–884) as a standalone node script against
  `.next/prerender-manifest.json` produced 14:31 today by the fixer's armed
  run of this identical tracked tree → `evidence-via: routes` /
  `PASS — all nine homes proven static`. Direct inspection of that artifact
  confirms the evidence law's three claims: `routes` is an OBJECT keyed by
  route; there is deliberately NO `"/"` key (bare pivot composed from `/en`
  by the §3 hop); the nine locale-scoped keys `/en /de /es /fr /ko /pl
  /pt-br /ru /zh` are each `"routeType":"page", "compute":"static"`.
  Dist-document fallback branch reads soundly and fails loud if the shape
  ever moves.
- **No-regression run**: five F4-era suites via the current runner →
  **Test Files 5 passed (5) / Tests 62 passed (62)** — tokens canon 24
  (byte-unmodified since import) + shell-hygiene 11 + chrome-parity 10 at
  its new `src/i18n/` home + locale-routing 11 + routes-manifest 6. Re-run
  again after the concurrent F5 builder's live `search-slot.tsx` edit landed
  mid-review: still 62/62.
- **m-4 on the real source**: comments-and-strings-stripped copy of
  `request.ts` still contains `notFound(` → the tightened grep passes for
  the right reason (a comment alone can no longer satisfy it).
- **m-7 environment half**: `netstat` shows no :3777 or :3000 listener —
  the review-era orphan state is gone, nothing to kill.
- Disk note: C: held ~483 MB free during this review with concurrent F5
  builders active, so I did **not** re-run a fresh `next build`; the M-2
  verification leans on the same-tree artifact above plus independent
  execution of the assertion itself. A fresh armed BUILD-leg re-run is
  cheap to redo once disk stabilizes (residual R5).

## Verification against the six brief items

1. **M-1 new law** — `STOCK_PALETTE =
   /(?:^|-)(?:slate|gray|zinc|neutral|stone)-(\d{2,3})$/` applied AFTER
   `baseClass()` strips variants/opacity/important, shade-ladder gate kept
   (50–950 step 50). Matches every prefixed/variant/opacity/important
   spelling, still bans bare stock tokens, leaves `bg-bg-1` /
   `--my-gray-900`-style names legal. Red-first re-proven on my own rig
   (above). GENUINELY FIXED.
2. **M-2 build leg** — guarded by env-var or flag, SKIP line when
   disarmed, disarmed-by-default holds; runs `next build` then asserts nine
   homes in Next 16 manifest form (`routes` object, `routeType:"page"`),
   with `<id>.html` fallback; executes after dev-server teardown (main()
   ordering verified); build failure and missing homes both FAIL loud;
   never silent-passes. Assertion semantics verified against a real
   artifact and by independent execution. FIXED, evidence law CORRECT for
   Next 16.
3. **m-1…m-9** — spot-verified four in depth (m-4, m-6, m-7, m-8 below);
   the remaining five verified by full code-read against their ledger rows:
   m-1 membership is scoped to the combobox's own `<nav aria-label>` row
   with EXACT expected-set comparison; m-2's `pagesGuard` opens all seven
   named absence legs (plus switch-links) with a loud vacuous-PASS refusal;
   m-3 binds section-nav anchors to `routes-manifest.ts` live rows resolved
   per locale (missing+extra, unreadable manifest = FAIL); m-5 `VAR_FORM`
   is bare-`var()` only, applied at both the canon-literal gate and the
   theme-aliasing gate; m-9's invented input/button demand is deleted,
   structural one-host/no-stray-text exactness kept. ALL CLOSED AS LEDGERED.
   Spot-check details:
   - **m-4** — stripped-code `notFound(` proven present on the real
     request.ts; prose/comment satisfaction impossible.
   - **m-6** — `chrome-parity.test.ts` renamed `site/messages/` →
     `site/src/i18n/` (git rename-detected, subject paths re-pointed via
     `join(here,"..","..","messages")` which resolves correctly); listed
     green at the new path in both my runs. Note: F5's `vitest.config.ts`
     has since replaced zero-config discovery; m-6's claim stays
     mechanically true because the pre-F5 suites run unchanged inside its
     node project — zero-config itself can no longer be observed while that
     config sits untracked in the tree (residual R3).
   - **m-7** — code half real: `killTree` is spawnSync taskkill returning
     status, main awaits actual exit and WARNs naming the one-dev-per-dir
     poisoning risk; `freePort` re-binds its draw as tripwire and redraws
     up to 5×; boot-timeout error names the lock and the netstat remedy.
     Environment half: orphan gone (no :3777/:3000 listener).
   - **m-8** — `flatten()` maps JSON `null` → `""`, and chrome-parity:164
     asserts every value a non-empty string naming key+file, so nulls now
     fail loud instead of passing as the string "null".
4. **N1/N3** — `hrefLang: locale.bcp47` rides the §2 BCP-47 column
   (`pt-BR`, `zh-Hans` confirmed in `locales.ts`, same source as the
   `<html lang>` law), one line + comment; `.wt-band-header` border-image
   slice rides `var(--tex-slice-header)` (token declared at tokens.css:197),
   duplicated literal gone. Both MINIMAL and CORRECT.
5. **No regression** — 62/62 F4-era tests green incl. tokens canon on the
   current tree (twice, bracketing a concurrent F5 edit). All six declared
   surfaces byte-identical to commit `6bd451d`. Tracked drift since the fix
   commit is other pieces' live work only: `EXTRACTION-LOG.md`,
   `run_all.ps1` (pipeline agent), `search-slot.tsx` (F5 builder,
   uncommitted). Full-suite reds and tsc errors today are exclusively in
   F5-owned files absent from the fix commit (residual R2/R4).
6. **First-build-honest-FAIL story** — the corrected evidence law is right
   for Next 16: `prerender-manifest.json` really carries `routes` as an
   object without a `"/"` key while emitting the nine locale artifacts
   (verified on the artifact itself), so asserting the nine locale-scoped
   entries — never `"/"` — is the correct executable form, with the
   documented dist-document fallback guarding future shape moves. The
   ledger's honest-FAIL-then-green sequence is consistent with this shape
   and the fail-loud paths are structurally guaranteed.

## Residuals (enumerated; none reopens an r1 finding)

| id | severity | residual |
|---|---|---|
| R1 | NOTE | Fix commit `6bd451d` swept three F5 §9 devDependencies into `site/package.json` (+3 lines: @testing-library/react, @vitejs/plugin-react, jsdom) beyond the six declared surfaces. Inert for F4 suites (vitest pre-existed; the others were unused until F5 files landed). Orchestration artifact in the c4f312f cross-pair mold — record, don't revert. |
| R2 | NOTE | Full `npm test` is red TODAY (7 failed / 134 passed across 10 files) and `npx tsc --noEmit` fails — every failure lives in F5-owned WIP files absent from `6bd451d` (`searchRows.test.ts` ×2 projects, `SiteSearchField.test.tsx` parse errors, `search-artifact.test.ts`). Zero F4-era failures; attribution, not regression. |
| R3 | NOTE | Untracked F5 `vitest.config.ts` replaces zero-config discovery. Pre-F5 suites keep running unchanged in its node project (verified), so m-6's substance holds; but "discovered by the untouched zero-config run" is no longer observable while that file sits in the tree. |
| R4 | NANO | F5's tsconfig exclusion covers `**/*.test.ts` only, so F5's `.tsx` component test leaks into `tsc --noEmit` and breaks it (source of R2's tsc leg). F5-scope duty per its own spec preamble — flagged for the F5 pair, not the F4 fix round. |
| R5 | PROCESS | Fresh armed BUILD-leg re-run deferred: C: at ~483 MB free with concurrent builders live made a fresh `next build` reckless. M-2 was instead verified against the same-tree `.next` artifact + independent assertion execution. Re-run `WARTALES_SMOKE_BUILD=1 node scripts/smoke.mjs` once disk stabilizes for the belt-and-braces pass. |
| R6 | NANO | `switch-links` filters offered hrefs to the locale-home set before computing "unexpected", so a stray NON-locale link inside the combobox row would not be flagged there (it remains caught by curl-greetable's nav-equality / search-ownership). A hair short of the ledger's "unexpected links both flagged" wording; m-1's substance (drop-a-locale can no longer hide) fully delivered. |

## Count table

| measure | count |
|---|---|
| r1 MAJORs genuinely fixed | 2 / 2 (M-1 re-proven red-first; M-2 asserted against real Next 16 artifact) |
| r1 MINORs closed as ledgered | 9 / 9 (spot-verified in depth: m-4, m-6, m-7, m-8; remaining five code-read against ledger) |
| code minors fixed (N1/N3) | 2 / 2, minimal + correct |
| F4-era suites/tests on current tree | 5 files / **62/62 green** (incl. tokens canon 24 unmodified) |
| disarmed smoke | PASS — 1 passed, 0 failed, 19 skipped, exit 0 |
| declared-surface drift vs `6bd451d` | 0 bytes |
| residuals | 0 blocking · 4 notes · 2 nano |

**Verdict stands: DELTA-CLEAN.**
