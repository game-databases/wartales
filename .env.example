# PostHog product analytics
# Collector: Game Databases, numeric project id 536998
# Do not use New World Guide project 557596.
#
# Public write key — set locally or in host env. Never commit a real token.
# Empty = fail closed (SDK does not init; no events).
# Next.js reads this from site/.env.local (copy these keys there).
NEXT_PUBLIC_POSTHOG_KEY=
#
# Ingest is direct US cloud (no first-party reverse proxy on this site):
#   api_host  https://us.i.posthog.com
#   ui_host   https://us.posthog.com
