/**
 * Public index route so the App Router has a page that can emit $pageview.
 * Copy stays empty — P3 site content is not part of this analytics wiring.
 */
export default function HomePage() {
  return <main className="wartales-home-main" />
}
